---
slug: landing-reposicionamiento
status: done
owner: nicolas
created: 2026-07-25
related_adrs: [ADR-0005, ADR-0006, ADR-0008, ADR-0014]
related_context: [Tienda, Organizador, Sorteo]

features:
  - id: F01
    behavior: "El copy de la landing cuenta la historia sorteo-first (hero, pasos, momento, confianza, CTA final) sin la palabra 'rifa' ni menciones de Flow, y el tagline de APP_CONFIG pasa a 'Monta tu sorteo online tú mismo, en un día.'"
    state: passing

  - id: F02
    behavior: "La landing suma las secciones 'hazlo tú mismo' (BLANCA) y 'precio' (AMARILLA) en la secuencia de 9 bandas aprobada, sin blancas adyacentes y sin recolorear secciones existentes; design.md registra la secuencia nueva."
    state: passing

  - id: F03
    behavior: "La FAQ tiene las 9 entradas aprobadas en su orden, con el precio real, la única mención de Flow de la página y las respuestas honestas de producto (1 sorteo activo por tienda, solo PDF hoy)."
    state: passing

  - id: F04
    behavior: "La landing sirve JSON-LD válido (FAQPage + Organization + WebSite + SoftwareApplication/Offer $25.000 CLP), canonical, og:locale es_CL y meta keywords (única superficie visible-para-máquinas donde se permite 'rifa')."
    state: passing

  - id: F05
    behavior: "El apex sirve robots.txt y sitemap.xml, y el og:image pasa a un PNG 1200×630 real con URL absoluta."
    state: passing
---

# Reposicionamiento sorteo-first de la landing del apex

## Contexto

La landing oficial del apex (`src/components/landing/`) cuenta la historia al revés: se presenta como "tienda para vender PDFs + sorteos" cuando el producto es una plataforma para montar sorteos online donde lo vendido es el vehículo para participar. Además no aparece el diferenciador clave — **autoservicio total en 1 día** (la competencia de referencia vende sitios de rifas a medida desde $650.000 CLP + 1–2 semanas + cotización; Sortéatelo es suscripción mensual barata y lo montas tú mismo), y "Flow" contamina el copy público (el chileno común no sabe qué es). El diagnóstico fue validado por el usuario el 2026-07-25.

Esta iteración reescribe el copy (sorteo-first), agrega dos secciones (precio y hazlo-tú-mismo), reescribe la FAQ con el modelo comercial real recién fijado ($25.000/mes, configura gratis, cero comisión por venta), y deja el apex con SEO técnico de primer nivel (JSON-LD, robots, sitemap, canonical, OG raster). Restricción legal no negociable (ADR-0008): la mecánica descrita es siempre "compras un producto → participas en el sorteo promocional" — nunca venta directa de números de rifa. El usuario pule el copy fino después: este plan fija estructura, mensajes y borrador de copy, no el texto definitivo palabra por palabra.

## Decisiones

- **D1 — Solo "sorteo", cero "rifa" en todo el copy visible** (Q1, opción máxima seguridad). Razón: coherencia con la marca Sortéatelo y con ADR-0008; el usuario acepta perder el puente coloquial con "rifa". Ver D13 para la única excepción (metadata invisible).
- **D2 — Audiencia amplia: el organizador de sorteos en general** (Q2). Eyebrow tipo "Organiza sorteos online · Chile" (borrador). El requisito de producto digital se calza en pasos y FAQ, no en el eyebrow; el live deja de ser identidad de audiencia y queda como el momento del sorteo (banda amarilla existente).
- **D3 — Plan único, $25.000 CLP/mes, IVA incluido, sin nombre de fantasía** (Q3). El precio se imprime real en la landing — muere el "en definición" de la FAQ actual.
- **D4 — Promo multi-tienda: 2ª tienda en adelante a mitad de precio ($12.500/mes c/u)** (Q4A). Copy: "¿Otra tienda? La segunda en adelante, a mitad de precio." Sin tablas ni asteriscos.
- **D5 — Remate coloquial del precio: "Menos de mil pesos al día."** (Q4B). Sobrio, verificable ($25.000/30 ≈ $833), no depende de precios ajenos.
- **D6 — Modelo "configura gratis, pagas cuando publicas"** (Q5). "Gratis para partir · Sin tarjeta" SOBREVIVE en el hero; la tarjeta de precio aclara "Configura tu tienda gratis. El plan corre cuando publicas." **DECISIÓN HEREDABLE fijada por el usuario: el gate de cobro del futuro carril de billing = la PUBLICACIÓN de la tienda.**
- **D7 — CTA "Crea tu tienda gratis" en todos los puntos** (Q6): header, hero, boleto final. Login queda como está. El sorteo-first vive en titular/bajada; el botón describe la acción real del click.
- **D8 — Secuencia de bandas (9)** (Q7): AZUL hero → BLANCA cómo funciona → AMARILLA momento → BLANCA hazlo-tú-mismo → AMARILLA precio → BLANCA confianza → GRIS FAQ → AZUL boleto → TINTA footer. Cero recolores de secciones existentes; el precio recibe el amarillo lotería. `docs/design.md` §9 debe actualizarse con la secuencia nueva (aprobada por el usuario en Q7).
- **D9 — Promesa económica: "cero comisión por venta — pagas el plan fijo y punto"** (Q8), con honestidad del procesador (cobra su tarifa por transacción, la ves en tu propia cuenta, no pasa por nosotros). Invariante I4 abajo.
- **D10 — Flow exactamente 1 vez en toda la landing** (Q9): dentro de la FAQ "¿Cómo me llega la plata?", siempre como "el procesador". Nunca en hero/pasos/confianza/CTAs. El paso de pagos se redacta para que conectar la cuenta quepa en el "todo esto en una tarde".
- **D11 — Tagline nuevo en `APP_CONFIG`: "Monta tu sorteo online tú mismo, en un día."** (Q10). Alimenta title/meta description/OG.
- **D12 — FAQ: 9 entradas en el orden aprobado** (Q11; lista completa en el Plan). Título de sección "Lo que preguntaría tu yo de hace cinco minutos" SE MANTIENE. "¿Puedo hacer más de un sorteo?" responde honesto: uno ACTIVO a la vez por tienda (guard secuencial real), con la 2ª tienda a mitad de precio como salida.
- **D13 — Paquete SEO dentro de la task + "rifa" SOLO en metadata invisible** (Q12): JSON-LD (FAQPage, Organization, WebSite, SoftwareApplication con Offer $25.000 CLP), robots.txt, sitemap.xml, canonical, og:locale, OG PNG 1200×630 con URL absoluta, jerarquía h1/h2 verificada. La palabra "rifa" queda permitida ÚNICAMENTE en `<meta name="keywords">` y en la propiedad `keywords` del JSON-LD — ambas invisibles para el lector. **Honestidad técnica registrada: `meta keywords` es ignorado por Google desde ~2009 y el `keywords` de JSON-LD no es factor de ranking confirmado — su valor es ~cero; la captura real del volumen de búsqueda "rifa" es la guía de contenido futura** ("¿Rifa o sorteo? Lo que dice la ley en Chile"), registrada como task futura, fuera de este alcance.

## Plan

1. **Reescribir `src/components/landing/copy.ts`** con el copy sorteo-first (F01). Borrador de mensajes (el usuario pule el texto fino después):
   - HERO — eyebrow: "Organiza sorteos online · Chile". Titular (plumón en las palabras marcadas): "Monta tu **sorteo** online. **Hoy** mismo." Bajada: "Con Sortéatelo armas tú mismo la tienda de tu sorteo: subes lo que vas a vender, activas pagos con tarjeta y compartes el enlace — todo en un día, sin programadores ni cotizaciones." (nombre vía `APP_CONFIG.name`). Nota bajo CTA se mantiene: "Gratis para partir · Sin tarjeta".
   - PASOS (historia del "en un día"): ① "Crea tu tienda" (plantilla, logo, colores — listo) ② "Sube lo que vas a vender" (tu producto digital: novela, fanzine, guía) ③ "Activa pagos con tarjeta" (conectas tu cuenta y cada peso llega directo a ti) ④ "Comparte y sortea en vivo" (cada compra da un número; el ganador sale frente a todos). Remate explícito de la sección: "Todo esto, en una tarde."
   - MOMENTO: se mantiene en espíritu (sorteo frente a todos); retoques menores de coherencia.
   - CONFIANZA: tarjeta [0] sin Flow → "Tu plata nunca pasa por nosotros — cobras directo en tu cuenta, con pago con tarjeta (Webpay)." Tarjetas [1] y [2] se mantienen. Bajada de la intro puede sumar el eco "cero comisión por venta".
   - CTA FINAL: "sube tu primer PDF" → "sube tu producto"; resto del boleto se mantiene.
   - `src/config/app.ts`: tagline → "Monta tu sorteo online tú mismo, en un día."
2. **Nuevas secciones en `landing-plataforma.tsx` + copy** (F02): banda BLANCA "hazlo tú mismo" (eyebrow tipo "Hazlo tú mismo"; 3 mensajes anti-agencia sin nombrar competencia: nadie te manda un presupuesto / no dependes de un programador / partes hoy, no en dos semanas) y banda AMARILLA "precio" (título "Un solo plan, todo incluido" · $25.000/mes IVA incluido · qué incluye: tienda en tu subdominio, pagos con tarjeta, sorteos, entrega automática · "Configura tu tienda gratis. El plan corre cuando publicas." · "¿Otra tienda? La segunda en adelante, a mitad de precio." · "Cero comisión por venta — pagas el plan fijo y punto." · remate "Menos de mil pesos al día."). Insertarlas en la secuencia D8; verificar h1 único + h2 por sección; reusar `Banda`/`Etiqueta`/`Card`/gramática talonario existente (cero hex, CSS module solo vars del theme). Actualizar `docs/design.md` §9 con la secuencia nueva de bandas.
3. **FAQ nueva (9 entradas, orden aprobado)** en `copy.ts` (F03): ① ¿Cuánto cuesta? (gratis configurar → $25.000/mes IVA incluido al publicar → 2ª tienda mitad de precio → cero comisión por venta; el procesador cobra su tarifa por transacción y la ves en tu propia cuenta) ② ¿Qué puedo vender? (productos digitales en PDF hoy) ③ ¿Necesito saber de páginas web? (no: plantilla + logo + colores) ④ ¿Cómo me llega la plata? (directo a tu cuenta, no pasa por nosotros; **única mención de Flow**, como procesador; la cuenta se crea gratis al configurar, te guiamos) ⑤ ¿Cómo sabe el comprador su número? (pantalla + correo, al confirmarse el pago) ⑥ ¿Cómo se elige al ganador? (se mantiene) ⑦ ¿Puedo hacer más de un sorteo? (todos los que quieras, uno a la vez por tienda; varias tiendas = mitad de precio) ⑧ ¿Qué pasa si un pago falla? (reescrita sin "Flow") ⑨ ¿Necesito iniciar actividades en el SII…? (se mantiene).
4. **JSON-LD + metadata on-page** (F04): componente/helper que derive de `copy.ts`/`APP_CONFIG` los bloques `FAQPage` (9 entradas 1:1), `Organization`, `WebSite` y `SoftwareApplication` con `Offer` (price 25000, priceCurrency CLP, billing mensual); `<link rel="canonical" href="https://sorteatelo.cl/">`; `og:locale` es_CL; `<meta name="keywords">` con "sorteo online, sorteos online Chile, rifa online, rifas" (D13 — dejar comentario en código: valor de ranking ~cero, decisión consciente). Solo en la landing del apex — no tocar el despacho por tenant.
5. **SEO técnico de archivos** (F05): `public/robots.txt` (allow all + `Sitemap: https://sorteatelo.cl/sitemap.xml`), `public/sitemap.xml` estático del apex, rasterizar `og.svg` → `public/og.png` 1200×630 y apuntar `og:image` a la URL ABSOLUTA (`https://sorteatelo.cl/og.png`). Cierra el pendiente "OG raster" de design.md (marcarlo).
6. Verificación visual en :3001 (bandas, precio, FAQ) + revisión del usuario sobre el copy borrador.

## Validaciones

### F01 — Copy sorteo-first + tagline

**Vitest** (integration):
- [x] Ningún string exportado por `copy.ts` (ni el tagline de `APP_CONFIG`) contiene "rifa"/"Rifa" (D1). — `src/__tests__/components/landing-copy.test.ts::landing.copy.001` ✅ 2026-07-26
- [x] "Flow" no aparece en HERO, PASOS, MOMENTO, CONFIANZA, CTA_FINAL ni FOOTER (D10). — `src/__tests__/components/landing-copy.test.ts::landing.copy.002` ✅ 2026-07-26
- [x] Toda ocurrencia de "comisión/comisiones" en el copy lleva el apellido "por venta" o "nuestras" (I4). — `src/__tests__/components/landing-copy.test.ts::landing.copy.003` ✅ 2026-07-26
- [x] El copy no usa lenguaje de urgencia ni escasez (design.md §8) — cubre el vocabulario típico, no la escasez implícita. — `src/__tests__/components/landing-copy.test.ts::landing.copy.005` ✅ 2026-07-26
- [x] `APP_CONFIG.tagline` es exactamente "Monta tu sorteo online tú mismo, en un día." (D11). — `src/__tests__/config/app.test.ts::config.app.003` ✅ 2026-07-26

**E2E** (browser):
- [x] El hero del apex muestra el titular sorteo-first, el eyebrow amplio, la nota "Gratis para partir · Sin tarjeta" y el CTA "Crea tu tienda gratis" apuntando a /login. — `tasks/e2e-landing-reposicionamiento.md#landing.hero.001` ✅ 2026-07-26 (3 CTAs → /login; `\brifa\b` = 0; Flow = 1)

### F02 — Secciones nuevas + secuencia de bandas

**Vitest**:
- [x] El copy de la sección precio contiene "$25.000", "IVA incluido", la línea de la 2ª tienda a mitad de precio, "El plan corre cuando publicas" y "Menos de mil pesos al día." (D3–D6). — `src/__tests__/components/landing-copy.test.ts::landing.precio.001` ✅ 2026-07-26
- [x] El monto vive como NÚMERO único (`montoMensualClp` = 25000) y la UI lo formatea con `clp()` — fuente del `Offer` de F04 (I10). — `src/__tests__/components/landing-copy.test.ts::landing.precio.002` ✅ 2026-07-26
- [x] El copy de hazlo-tú-mismo no nombra competencia alguna (ni "r3q" ni marcas). — `src/__tests__/components/landing-copy.test.ts::landing.hazlo.001` ✅ 2026-07-26

**E2E**:
- [x] La landing renderiza las 9 bandas en el orden D8 exacto, sin dos blancas adyacentes, y las secciones nuevas se ven correctas en desktop y móvil. — `tasks/e2e-landing-reposicionamiento.md#landing.bandas.001` ✅ 2026-07-26 (orden D8 confirmado por color computado a 375×812; sin desborde horizontal; residuo móvil cerrado por la sesión principal)

### F03 — FAQ nueva

**Vitest**:
- [x] `FAQ` tiene exactamente 9 entradas en el orden aprobado (D12). — `src/__tests__/components/landing-copy.test.ts::landing.faq.001` ✅ 2026-07-26
- [x] "Flow" aparece exactamente 1 vez en TODO el copy de la landing, y es dentro de la respuesta "¿Cómo me llega la plata?" (D10). — `src/__tests__/components/landing-copy.test.ts::landing.faq.002` ✅ 2026-07-26
- [x] La respuesta de "¿Cuánto cuesta?" contiene el precio real y NO contiene "en definición". — `src/__tests__/components/landing-copy.test.ts::landing.faq.003` ✅ 2026-07-26
- [x] Las respuestas de producto son honestas (I9): un sorteo ACTIVO a la vez por tienda, solo PDF hoy. — `src/__tests__/components/landing-copy.test.ts::landing.faq.004` ✅ 2026-07-26
- [x] El copy NO promete que el número de cada compra sea visible en ninguna pantalla — esa superficie no existe ni para el Comprador ni en el panel (I9, bug real encontrado en review). — `src/__tests__/components/landing-copy.test.ts::landing.faq.005` ✅ 2026-07-26

**E2E**:
- [x] El Accordion muestra las 9 preguntas y las respuestas se expanden. — `tasks/e2e-landing-reposicionamiento.md#landing.faq.001` ✅ 2026-07-26 (click real: `aria-expanded` true, panel 237px, respuesta 1 verbatim; residuo cerrado por la sesión principal)

### F04 — JSON-LD + metadata

**Vitest**:
- [x] El JSON-LD `FAQPage` refleja 1:1 las 9 preguntas/respuestas de `copy.ts` (derivado, no duplicado a mano) y parsea como JSON válido. — `src/__tests__/components/landing-seo.test.ts::landing.seo.001` + `::landing.seo.002` ✅ 2026-07-26
- [x] El JSON-LD `SoftwareApplication/Offer` declara price 25000 y priceCurrency CLP. — `src/__tests__/components/landing-seo.test.ts::landing.seo.003` ✅ 2026-07-26
- [x] "rifa" NO aparece en title, meta description ni OG — solo en meta keywords / JSON-LD keywords (D13). — `src/__tests__/components/landing-seo.test.ts::landing.seo.004` ✅ 2026-07-26
- [x] Canonical y og:image son URLs ABSOLUTAS al dominio de `APP_CONFIG`, y la description entra en 160 caracteres. — `src/__tests__/components/landing-seo.test.ts::landing.seo.005` + `::landing.seo.006` ✅ 2026-07-26

**E2E**:
- [x] El HTML servido del apex incluye los scripts `application/ld+json`, el canonical `https://sorteatelo.cl/` y `og:locale` es_CL (y nada de eso aparece en las páginas de tenant). — `tasks/e2e-landing-reposicionamiento.md#landing.jsonld.001` + `#landing.metadata.001` ✅ 2026-07-26 (4 bloques en orden, Offer 25000 CLP, control negativo del tenant limpio)

### F05 — robots + sitemap + OG raster

**Vitest**:
- [x] `public/robots.txt` y `public/sitemap.xml` existen y el sitemap referencia el apex con URL absoluta. — `src/__tests__/components/landing-seo.test.ts::landing.seo.007` + `::landing.seo.008` ✅ 2026-07-26
- [x] `public/og.png` existe, es un PNG real y mide exactamente 1200×630 (leído de la cabecera IHDR). — `src/__tests__/components/landing-seo.test.ts::landing.seo.009` ✅ 2026-07-26

**E2E**:
- [x] `GET /robots.txt` y `GET /sitemap.xml` responden 200 en el dev server; `og:image` apunta a una URL absoluta `.png` y el archivo existe (1200×630). — `tasks/e2e-landing-reposicionamiento.md#landing.robots.001` + `#landing.og.001` ✅ 2026-07-26 (robots 200 text/plain, sitemap 200 xml, og.png 200 image/png byte-idéntico a disco)

## Invariantes

- I1: **ADR-0008** — la mecánica descrita es SIEMPRE "cada compra participa en el sorteo promocional"; PROHIBIDO prometer venta de números de rifa, en cualquier superficie.
- I2: "rifa" PROHIBIDO en toda superficie visible (copy, headings, title, meta description, OG, JSON-LD visible en rich results); permitido SOLO en `<meta name="keywords">` y `keywords` de JSON-LD (D13).
- I3: "Flow" exactamente 1 vez en la landing, en la FAQ "¿Cómo me llega la plata?", como "el procesador" (D10).
- I4: nunca prometer "sin comisiones" sin el apellido "por venta" o "nuestras" (D9).
- I5: secuencia de bandas D8 exacta; dos blancas nunca adyacentes (design.md §9); si se toca la secuencia, actualizar design.md.
- I6: NO tocar el despacho por zona/tenant de `src/pages/index.tsx` ni el storefront de tenants — esta task es solo la superficie del apex.
- I7: cero hex inline; CSS module de landing solo con vars del theme; Tailwind solo layout (design.md §9). Marca/dominio SIEMPRE desde `APP_CONFIG` (nunca literal "Sortéatelo" en JSX).
- I8: voz design.md §8 — tuteo chileno sobrio, sin lenguaje de urgencia/escasez; no justificar recortes "porque es MVP" (el producto ya no lo es).
- I9: no prometer features inexistentes: el billing NO existe (la landing declara el modelo D6, no un flujo de pago de suscripción); los sorteos son 1 activo a la vez por tienda.
- I10: el JSON-LD se DERIVA de `copy.ts`/`APP_CONFIG` — prohibido duplicar los textos a mano (dos verdades divergen).

## Out of scope

- Implementar el cobro de la suscripción (carril de billing futuro; hereda D6: gate = publicación).
- Guía de contenido "¿Rifa o sorteo?" y toda estrategia de contenido programático (task futura registrada en D13).
- SEO de las tiendas de tenants, Search Console, analytics, link building.
- Cambios al login (copy o layout), al panel, al storefront de tenants.
- Logo/isotipo dibujado (pendiente aparte de design.md).
- Texto legal definitivo (validación de abogado = F10 del roadmap).

## Especialistas a consultar

- `frontend-reviewer` — las bandas nuevas, la gramática talonario y las reglas de design.md §9.
- `change-set-reviewer` — cierre del diff (incluye `docs/design.md` y `public/`).
- `feature-tester` — E2E browser de la landing (bandas, FAQ, metas, robots/sitemap) — recomendado: sí toca UI.

## Bitácora

- [2026-07-25 21:05] [planner-grill] Contexto cargado: copy.ts completo (hero/pasos/confianza/FAQ/CTA), landing-plataforma.tsx (secuencia de bandas D9 + regla I6), design.md §8 voz (tuteo chileno, SIN lenguaje de urgencia/escasez de rifa en chrome de plataforma — bandera de estafa) y §9 reglas duras, APP_CONFIG (tagline actual "Vende lo que hiciste y sortéalo entre quienes te compraron"). Flow aparece en: hero.bajada, PASOS[1], CONFIANZA[0], FAQ[3].
- [2026-07-25 21:05] [planner-grill] Q1: ¿Palabra dominante del copy: "rifa" o "sorteo"? Recomendada: "sorteo" dominante con "rifa" como puente acotado.
- [2026-07-25 21:20] [planner-grill] Q1 answered: opción (c) — SOLO "sorteo", CERO "rifa" en todo el copy de la landing. Máxima seguridad legal/marca; el usuario acepta perder el puente coloquial.
- [2026-07-25 21:20] [planner-grill] Q2: ¿A quién le habla la landing (eyebrow y voz del hero)? Recomendada: ampliar al organizador de sorteos en general.
- [2026-07-25 21:35] [planner-grill] Q2 answered: opción (b) — audiencia amplia, eyebrow tipo "Organiza sorteos online · Chile"; el requisito de producto digital se calza en pasos y FAQ; el live queda como momento del sorteo.
- [2026-07-25 21:35] [planner-grill] Q3: Precio — ¿monto exacto, plan único, nombre? Recomendada: plan único sin nombre, $20.000/mes IVA incluido.
- [2026-07-25 21:50] [planner-grill] Q3 answered: **$25.000/mes, plan único, sin nombre de fantasía, IVA incluido**. El usuario pide además: (a) promoción por 2ª/3ª tienda del mismo organizador — esquema a proponer por el planner; (b) remate coloquial "menos que..." en la sección de precio — a proponer en voz chilena sobria.
- [2026-07-25 21:50] [planner-grill] Q4: Esquema de la promo multi-tienda + remate "menos que...". Recomendada: mitad de precio 2ª tienda + "menos de mil pesos al día".
- [2026-07-25 22:05] [planner-grill] Q4 answered: Parte A opción (a) — 2ª tienda en adelante a mitad de precio ($12.500/mes c/u), línea "¿Otra tienda? La segunda en adelante, a mitad de precio.". Parte B opción (1) — remate "Menos de mil pesos al día."
- [2026-07-25 22:05] [planner-grill] Q5: ¿Sobrevive "Gratis para partir · Sin tarjeta" con $25.000/mes visibles? Recomendada: modelo "configura gratis, pagas cuando publiques".
- [2026-07-25 22:20] [planner-grill] Q5 answered: opción (a) — configura gratis / el plan corre cuando publicas. "Gratis para partir · Sin tarjeta" SOBREVIVE; tarjeta de precio aclara "Configura tu tienda gratis. El plan corre cuando publicas." **DECISIÓN HEREDABLE: el gate de cobro del futuro carril de billing = la PUBLICACIÓN de la tienda** (fijado por el usuario, registrar en Decisiones del plan).
- [2026-07-25 22:20] [planner-grill] Q6: ¿CTA principal? Recomendada: mantener "Crea tu tienda gratis" en todos los botones.
- [2026-07-25 22:35] [planner-grill] Q6 answered: opción (a) — "Crea tu tienda gratis" en header, hero y boleto final; login queda como está. Sorteo-first vive en titular/bajada.
- [2026-07-25 22:35] [planner-grill] Q7: ¿Secuencia de bandas con las 2 secciones nuevas? Recomendada: precio en AMARILLA propia tras hazlo-tú-mismo BLANCA, cero recolores.
- [2026-07-25 22:50] [planner-grill] Q7 answered: opción (a) — AZUL hero → BLANCA cómo funciona → AMARILLA momento → BLANCA hazlo-tú-mismo → AMARILLA precio → BLANCA confianza → GRIS FAQ → AZUL boleto → TINTA footer.
- [2026-07-25 22:50] [planner-grill] Q8: ¿Se mantiene "sin comisiones escondidas" con suscripción? Recomendada: reforzar a "cero comisión por venta" + honestidad del procesador.
- [2026-07-25 23:05] [planner-grill] Q8 answered: opción (a) — "cero comisión por venta — pagas el plan fijo y punto"; FAQ aclara la tarifa del procesador por transacción. **INVARIANTE DE COPY fijado: nunca prometer "sin comisiones" sin el apellido "por venta" o "nuestras".**
- [2026-07-25 23:05] [planner-grill] Q9: ¿Dónde queda la ÚNICA mención de Flow + cómo contar el requisito BYO-Flow? Recomendada: 1 mención en FAQ "¿Cómo me llega la plata?".
- [2026-07-25 23:20] [planner-grill] Q9 answered: opción (a) — **INVARIANTE: Flow exactamente 1 vez en toda la landing, dentro de la FAQ "¿Cómo me llega la plata?", siempre como "el procesador"**; nunca en hero/pasos/confianza/CTAs. Paso de pagos redactado para que conectar la cuenta quepa en el "todo esto en una tarde".
- [2026-07-25 23:20] [planner-grill] Q10: ¿Se actualiza APP_CONFIG.tagline? Recomendada: sí, "Monta tu sorteo online tú mismo, en un día."
- [2026-07-25 23:35] [planner-grill] Q10 answered: opción (a) — tagline nuevo "Monta tu sorteo online tú mismo, en un día." entra al alcance (src/config/app.ts).
- [2026-07-25 23:35] [planner-grill] Q11: Lista final y orden de la FAQ (9 propuestas: cuánto cuesta / qué puedo vender / saber de páginas web / cómo me llega la plata / número del comprador / cómo se elige ganador / más de un sorteo / pago falla / SII). Nota de producto: "más de un sorteo" debe responder honesto = uno ACTIVO a la vez por tienda (guard secuencial 1-ACTIVO de launch-crear-sorteo).
- [2026-07-26 00:00] [planner-grill] Q11 answered: las 9 entradas en el orden propuesto APROBADAS; el título de sección "Lo que preguntaría tu yo de hace cinco minutos" SE MANTIENE.
- [2026-07-25 23:50] [planner-grill] Usuario pregunta (antes de responder Q11): ¿se puede dejar esto con SEO de primer nivel para Chile? Hallazgos del repo: NO existen robots.txt ni sitemap.xml ni canonical; og:image apunta a `/og.svg` (SVG relativo — los crawlers sociales no lo renderizan Y necesita URL absoluta: 2 bugs); no hay JSON-LD; design.md ya listaba el OG raster PNG 1200×630 como pendiente.
- [2026-07-25 23:50] [planner-grill] Q12: Alcance SEO de ESTA task + tensión rifa/sorteo en keywords. Recomendada: paquete on-page+técnico acotado a la landing (JSON-LD FAQPage/Organization/WebSite/SoftwareApplication con Offer $25.000, robots.txt, sitemap.xml, canonical, OG PNG 1200×630 absoluto, jerarquía h1/h2 verificada) DENTRO de la task; keywords "rifa" opción (i) cero-rifa también en SEO, con task futura de contenido (guías) registrada como el vehículo legítimo para capturar búsquedas de "rifa".
- [2026-07-26 00:15] [planner-grill] Q12 answered: Parte A — paquete SEO completo APROBADO (entra a la task); lo futuro (guías, tenant SEO, Search Console) queda registrado como task futura. Parte B — recomendada (i)+(iii) aceptada CON adición del usuario: "rifa" SÍ entra como metadata invisible. Aterrizaje acordado con el orquestador: "rifa" permitido SOLO en `<meta name="keywords">` (ignorado por Google desde ~2009 — valor ~cero, dicho explícito) y `keywords` de JSON-LD (no factor de ranking confirmado, invisible en rich results); PROHIBIDO en title, meta description, OG, headings y todo copy visible. Captura real del volumen "rifa" = guía futura (D13).
- [2026-07-26 00:20] [planner-grill] GRILL CERRADO (12 rondas). Plan escrito: 5 features (F01 copy sorteo-first + tagline, F02 secciones nuevas + bandas, F03 FAQ ×9, F04 JSON-LD/metadata, F05 robots/sitemap/OG raster), 13 decisiones, 10 invariantes. Task futura a registrar cuando se abra: contenido SEO ("¿Rifa o sorteo? Lo que dice la ley en Chile" + guías). AWAITING USER APPROVAL.
- [2026-07-25 22:55] [feature-implementer] Visto bueno del usuario recibido ⇒ `status: planning → implementing`. Features pendientes: F01, F02, F03, F04, F05. Read pass completo: `_template.md`, `CLAUDE.md`, ADR-0008 (responsabilidad del sorteo), `docs/design.md` (§2 paleta / §4 gramática suave / §8 voz / §9 reglas duras + pendiente "OG raster"), `docs/agents/frontend-conventions.md`, `copy.ts`, `landing-plataforma.tsx`, `banda.tsx`/`etiqueta.tsx`/`landing.module.css`, `src/config/app.ts`, `src/pages/index.tsx` (despacho por zona — NO se toca, I6), `src/lib/formato.ts`, `_document.tsx` (`<html lang="es">`), `middleware.ts` (matcher deja pasar `public/`), `vitest.config.ts`. Herramientas verificadas para F05: Chrome headless en `C:\Program Files\Google\Chrome\Application\chrome.exe` + woff2 de marca en `videos/hyperframes/_brand/fonts/` ⇒ el raster del OG se hace SIN instalar dependencias nuevas (sharp/resvg NO están y su instalación requeriría permiso del usuario).
- [2026-07-25 23:00] [feature-implementer] **F01 implementada** (TDD red→green, 3 ciclos). Archivos: `src/config/app.ts` (tagline D11), `src/components/landing/copy.ts` (HERO sorteo-first con titular «Monta tu **sorteo** online. **Hoy** mismo.» + eyebrow amplio D2; COMO_FUNCIONA con `remate`/`remateDestacado`; PASOS ×4 nuevos con la mecánica ADR-0008 «cada compra participa en el sorteo con su número»; MOMENTO retocado; CONFIANZA_INTRO sin «rifas» + eco «no te cobra comisión por venta»; CONFIANZA[0] sin Flow; CTA_FINAL.bajada «sube tu producto»), `src/components/landing/landing-plataforma.tsx` (titular nuevo + remate), tests `src/__tests__/components/landing-copy.test.ts` (nuevo, 4 casos) y `src/__tests__/config/app.test.ts` (+`config.app.003`), `tasks/e2e-landing-reposicionamiento.md` (nuevo, check `landing.hero.001`). Vitest filtrado 7/7 verde; `tsc --noEmit` limpio.
- [2026-07-25 23:00] [feature-implementer] Decisiones tácticas de F01: (a) los tests leen los **exports** del módulo (aplanados), no el archivo crudo ⇒ sobreviven a una reescritura del copy; (b) el test de Flow de F01 se acota al RELATO (hero/pasos/momento/confianza/CTA/footer) porque el conteo global «exactamente 1» pertenece a F03, que crea la FAQ dueña de esa mención; (c) se corrigió YA el apellido «por venta» en la FAQ vieja «¿Cuánto cuesta?» — I4 es global y esa FAQ se reescribe entera en F03 igual; (d) el E2E vive en `tasks/e2e-landing-reposicionamiento.md` nuevo (no había archivo E2E de la landing; naming consistente con `e2e-admin-multi-tienda.md`).
- [2026-07-25 23:00] [feature-implementer] Auto-chequeo `docs/design.md` previo al reviewer: cero hex inline, cero clases de color/tipografía/sombra de Tailwind, marca siempre vía `APP_CONFIG`, voz §8 (tuteo, sin urgencia), motion §7 intacto, h1 único. **frontend-reviewer: APPROVE**, 0 blockers, 2 nits. NIT-1 aplicado: el remate reusaba el tratamiento tipográfico exacto del título de card (fw 600 / 17.5) y se leía como un 5º paso huérfano ⇒ pasó a fw 700 / 20 **con plumón** sobre «en una tarde.» (firma del talonario, design.md §1) — obligó a partir el copy en `remate` + `remateDestacado`. NIT-2 NO aplicado por estar fuera del plan (Plan paso 1: «resto del boleto se mantiene») ⇒ **REVISABLE para el usuario**: `CTA_FINAL.eyebrow` = "Admite a 1 (una) tienda nueva" se puede leer como escasez ("solo admitimos 1 tienda nueva"), que design.md §8 marca como bandera de estafa; leído como grafía de boleto ("Admite a 1") es la firma del talonario. Decidir en la pasada de copy fino.
- [2026-07-25 23:08] [feature-implementer] **F02 implementada.** Archivos: `src/components/landing/copy.ts` (+`HAZLO_TU_MISMO` con 3 mensajes anti-agencia sin nombrar a nadie; +`PRECIO` con `montoMensualClp: 25_000` como NÚMERO fuente única y `monto: clp(...)` — el `$` nunca se concatena a mano y el número crudo es lo que consumirá el `Offer` del JSON-LD en F04, I10), `src/components/landing/landing-plataforma.tsx` (banda BLANCA hazlo-tú-mismo + banda AMARILLA precio insertadas entre MOMENTO y CONFIANZA ⇒ secuencia D8 de 9 bandas, cero recolores), `docs/design.md` §9 (secuencia nueva registrada — cambio DECLARADO en el Plan, no drift). Vitest filtrado 7/7; `tsc` sin errores propios. **frontend-reviewer: APPROVE**, 0 blockers, 1 nit APLICADO (unificar el literal del color de ícono de CONFIANZA en la const `COLOR_ICONO`).
- [2026-07-25 23:08] [feature-implementer] Decisiones tácticas de F02: (a) "hazlo tú mismo" va SIN `Card` a propósito — queda entre dos secciones que sí las usan (pasos y confianza) y una tercera grilla de cards aplanaba el ritmo; lleva `Perforacion` bajo el título para dar textura de talonario; (b) el remate del precio usa `Plumon variante="b"`, que sobre banda amarilla conmuta a trazo BLANCO por el CSS module — consolida "remate de sección = frase con plumón" (2º caso, junto al de F01); (c) la card del precio es `w-full max-w-[380px]` (Tailwind de layout) en vez de un `style` nuevo en el componente `Card` local.
- [2026-07-25 23:10] [feature-implementer] ⚠ **Carril ajeno en vuelo en el MISMO árbol de trabajo**: `git status` muestra el retiro del Operador/MCP (`plataforma-retiro-operador`) a medio aplicar — `src/server/domain/operador/*`, `src/pages/admin/operador.tsx`, `src/app/api/mcp/*` borrados y `authPolicy`/`trpc.ts` modificados. Eso produce **45 errores de `tsc` AJENOS** (todos por `esOperador` fuera de `AccesoPanel`, en tests del server). **Cero errores en los archivos de esta task** (verificado con grep sobre la salida: ni `landing`, ni `config/app`, ni `copy`). No toco nada de ese carril. Consecuencia para el `feature-tester`: si `npm run check` sale rojo, contrastar primero contra esta nota — la corrida de esta feature es verde en su área.
- [2026-07-25 23:40] [feature-implementer] **F03 implementada.** `src/components/landing/copy.ts`: FAQ reescrita a las 9 entradas de D12 en el orden aprobado (precio real interpolado desde `PRECIO.monto` ⇒ muere "en definición"; única mención de Flow en «¿Cómo me llega la plata?» como *el procesador*; honestidad de producto: 1 sorteo ACTIVO a la vez por tienda y solo PDF hoy). `FAQ_INTRO` intacto (D12). Vitest 12/12 filtrado.
- [2026-07-25 23:40] [feature-implementer] ⚠ **BUG DE VERDAD ENCONTRADO EN REVIEW (2 rondas de REQUEST_CHANGES) — la landing prometía una feature inexistente.** El copy vivo decía que «quien compró **ve su número** en pantalla y en su correo» (`CONFIANZA[1]`, texto heredado que el Plan marcaba «se mantiene») y mi FAQ nueva lo repetía. **Verificado contra el código, NADIE ve el ordinal**: `src/pages/checkout/retorno.tsx` solo confirma el pago y anuncia el correo; `src/server/domain/correo/plantillaDescarga.ts` lleva enlaces + expiración + disclaimer, sin ordinal; `src/server/domain/checkout/getEstadoOrden.ts` ni siquiera lo expone (solo-estado, I-T6); no hay cuenta de Comprador (ADR-0004). Mi primer reemplazo introdujo una **segunda** mentira («los números los ves en tu panel, participante por participante»): `src/server/domain/panel/getSorteoDelPanel.ts` agrupa las entries por correo y devuelve **conteo de tickets**, no ordinales, y `src/pages/admin/sorteo.tsx` pinta Cliente / Tickets / Última participación. Y al escribir la 3ª versión detecté yo un tercer error propio: «nadie agrega participantes después» también sería falso, porque el Organizador SÍ puede arrastrar participantes de un sorteo anterior (modal de importar en `admin/sorteo.tsx`).
- [2026-07-25 23:40] [feature-implementer] Resolución del bug (I9 manda sobre el borrador de copy del Plan): (1) la pregunta 5 se REFORMULÓ a «¿Cómo sabe el comprador que su compra entró al sorteo?» — la anterior no tiene respuesta verdadera hoy — y su respuesta afirma solo lo verificable: entra sola al confirmarse el pago (`aplicarEfectosPostPago`), le llega el correo con la descarga (`conCorreoPostPago`), el conteo público se ve en la tienda (`getSorteoActivoStorefront` → `sorteo_vitrina`, sembrado por defecto en toda tienda nueva) y el panel muestra quién participó con cuántos tickets; (2) `CONFIANZA[1]` pasó a afirmar el REGISTRO automático del número correlativo, no su visibilidad. **Esto es una DESVIACIÓN CONSCIENTE del Plan** («Tarjetas [1] y [2] se mantienen»): dejar la tarjeta como estaba era dejar publicada una afirmación falsa, que I9 prohíbe. Revertirla es un cambio de una línea si el usuario prefiere otra salida; (3) test de regresión `landing.faq.005` con 3 patrones ESTRECHOS (verbo-de-ver + número adyacentes; número + superficie; número + pronombre + ver) — verificado que atrapa las dos mentiras históricas y que NO da falso positivo sobre dos frases que sí son ciertas («sortea entre esos números y te muestra el resultado», «en tu panel — sin números fantasma»); una ventana de proximidad genérica sí las rompía. **frontend-reviewer: APPROVE** en la 3ª pasada, con las 5 afirmaciones verificadas una por una contra el código.
- [2026-07-25 23:40] [feature-implementer] **REVISABLES para el usuario (NO tocados — fuera del alcance del plan, decisión de copy/producto):** (a) `MOMENTO.texto` dice «el ganador sale de un talonario que **toda tu comunidad puede ver**» y (b) `TESTIMONIO.cita` dice «cada compra queda con su número al tiro, y **mi comunidad lo ve**» — mismo patrón de riesgo que las dos mentiras corregidas: leídas literales prometen una superficie pública de números que no existe; leídas como metáfora del sorteo en vivo, pasan. El agravante de (b) es que la afirmación de producto vive dentro de una cita cuya atribución está «pendiente de autorización». `LOGIN.testimonio` NO tiene el problema (no incluye la cláusula de visibilidad). (c) La FAQ del SII promete «te dejamos una guía para partir» y no encontré esa guía en el repo ni en el ToS — texto heredado, confirmar si el recurso existe o va a existir. **Pregunta de fondo que le corresponde al usuario: ¿el Comprador DEBERÍA ver su número (carril nuevo de producto), o la landing se queda contando solo el registro automático?**
- [2026-07-26 00:05] [feature-implementer] **F04 implementada.** `src/components/landing/seo.ts` (NUEVO): metadata derivada — `URL_APEX`/`URL_CANONICA`/`URL_OG_IMAGEN` absolutas desde `APP_CONFIG.dominio`, `TITULO_SEO`, `DESCRIPCION_SEO` (arranca con el tagline, ≤160 chars), `KEYWORDS_SEO` y `LOCALE_SEO`, más `BLOQUES_JSON_LD` = Organization + WebSite + SoftwareApplication (con `Offer` 25000 CLP mensual, IVA incluido, `unitCode: MON`) + FAQPage. **I10 cumplido de verdad**: el `FAQPage` es un `.map` sobre `FAQ` y el precio del `Offer` es el MISMO `PRECIO.montoMensualClp` que pinta la sección de precio — cero texto duplicado a mano. `landing-plataforma.tsx`: `<Head>` completo (canonical, og:type/site_name/locale/url/title/description/image+width+height, twitter:card, keywords) + los 4 `<script type="application/ld+json">`. Vitest filtrado 18/18 (copy + seo); eslint limpio sobre `src/components/landing`; `tsc` sin errores propios.
- [2026-07-26 00:05] [feature-implementer] Decisiones tácticas de F04: (a) los `<script>` de JSON-LD van en el **BODY**, no dentro de `next/head` — es la forma que documenta Next para el pages router y evita la deduplicación que `next/head` aplica a tags especiales; el reviewer confirmó que es el PRIMER JSON-LD del repo (cero precedente que reconciliar); (b) 4 scripts separados en vez de un `@graph` (un bloque roto no invalida los otros 3), con el acoplamiento documentado: la referencia cruzada por `@id` entre scripts resuelve porque Google fusiona los bloques de la página; (c) `dangerouslySetInnerHTML` es seguro acá porque el contenido es copy NUESTRO (nada de tenant ni de usuario) y un test prohíbe `</script` en el JSON — si el patrón se reusa con datos de tenant habrá que escapar `<` de verdad. **frontend-reviewer: APPROVE**, 0 blockers, **3 nits APLICADOS**: quitar `Organization.logo` (era el banner OG 1200×630 y Google espera un logo cuadrado — se agrega cuando exista el isotipo), `inLanguage` al `FAQPage`, y documentar en el docblock la dependencia del merge de Google.
- [2026-07-26 00:30] [feature-implementer] **F05 implementada.** `public/robots.txt` (allow all + `Sitemap:` absoluto, tal como declara el Plan), `public/sitemap.xml` (una entrada: el apex, `<loc>` absoluto), `public/og.png` **1200×630 real**, `public/og.svg` actualizado, `docs/design.md` pendiente "OG raster" marcado `[x]` con la nota de cómo se regenera, `scripts/generar-og.mjs` + `npm run og:png`. Vitest filtrado **24/24** en los 3 archivos de la task (copy 12 + seo 9 + config app 3); eslint limpio sobre `src/components/landing`; `tsc` sin errores propios (el `.mjs` queda fuera del `include` de tsconfig, igual que los de `videos/hyperframes/`).
- [2026-07-26 00:30] [feature-implementer] Cómo se rasterizó el OG (F05) y **el defecto que apareció al hacerlo**: no hay `sharp` ni `resvg` en el repo y sumar dependencia requiere permiso ⇒ se usa **Chrome headless** (`--headless --screenshot --window-size=1200,630`) sobre un HTML que embebe el SVG + las **woff2 de marca en base64** desde `videos/hyperframes/_brand/fonts/`. Sin esa embebida el PNG sale en la serif del sistema. **Al verlo renderizado con Bricolage por primera vez se descubrió que el plumón del `og.svg` cruzaba «Tu sorteo.» como un TACHADO** (el rect terminaba justo en la línea de base): se corrigió a subrayado bajo el texto (y 432→470, alto 30→26, ancho 470→400) y se re-rasterizó. Verificado a ojo leyendo el PNG, no por ausencia de error. El `og.svg` mantiene la excepción de hex de `design.md` §9 y sus 3 hex siguen siendo los del theme.
- [2026-07-26 00:30] [feature-implementer] **frontend-reviewer F05: APPROVE**, 0 blockers, 4 nits. **2 aplicados**: (a) se quitó el `<lastmod>` del sitemap — es opcional y mantenido a mano queda viejo en la primera edición, y un lastmod que miente Google lo ignora; (b) el raster se versionó como `scripts/generar-og.mjs` + `npm run og:png` en vez de vivir solo como prosa en `design.md` — **verificado que reproduce el PNG byte-idéntico** (mismo md5). El script busca Chrome en rutas conocidas + `CHROME_PATH`, y falla con mensaje claro si faltan las woff2. Probado además que Chrome tolera la tilde de `NicolásChaima` en la ruta temp (el gotcha 11 de la fábrica de videos no aplica acá) ⇒ usa `os.tmpdir()`, sin hacks de ruta.
- [2026-07-26 00:30] [feature-implementer] **2 nits de F05 NO aplicados, elevados al usuario:** (a) `src/pages/login.tsx` sigue con `og:image` **relativo a `/og.svg`** — es exactamente el bug que F05 corrigió en la landing (SVG + ruta relativa = tarjeta social rota), pero el Plan declara "Cambios al login" en **Out of scope**, así que no lo toqué: es un follow-up de UNA línea (`content={URL_OG_IMAGEN}`); (b) el `robots.txt` de `public/` se sirve también en los subdominios de Tienda, y ahí su `Sitemap:` apunta al apex — inofensivo hoy (el sitemap solo lista el apex y el panel está gateado server-side, no por robots), pero cuando se abra el SEO de tenants va a hacer falta un robots/sitemap por subdominio. El reviewer confirmó además que `/editor`, `/bases`, `?preview=` y `/dev/descargas` YA emiten `noindex` propio; `/login` y `/admin/*` no, y para `/login` puede valer la pena (bajo valor SEO, sin dato sensible).
- [2026-07-26 00:35] [feature-implementer] **DRIFT DE DOCUMENTACIÓN detectado — 2 drafts SIN APLICAR (esperan permiso del usuario, Step 4.5).** Ambos para `docs/agents/frontend-conventions.md`, sección nueva al final:
  ```diff
  +## Metadata SEO de la landing del apex
  +
  +- La metadata de la landing vive en **`src/components/landing/seo.ts`**, sibling de `copy.ts`, no
  +  dentro del componente: son DATOS derivados y el `<Head>` solo los imprime. Es el primer (y por
  +  ahora único) módulo de SEO del repo — el storefront de tenants NO lo importa (alcance apex).
  +- **El JSON-LD se DERIVA del copy, nunca se escribe a mano.** El `FAQPage` es un `.map` sobre el
  +  array `FAQ` y el precio del `Offer` es el mismo número que la UI formatea con `clp()`. Un bloque
  +  escrito aparte se desincroniza en la primera edición y deja a Google mostrando en sus resultados
  +  una respuesta que la página ya no dice.
  +- Los `<script type="application/ld+json">` van en el **BODY**, no dentro de `next/head`: es la
  +  forma que documenta Next para el pages router y evita la deduplicación que `next/head` aplica a
  +  los tags especiales. `dangerouslySetInnerHTML` es seguro mientras el contenido sea copy NUESTRO
  +  (un test prohíbe `</script` en el JSON); **si algún día lleva datos de tenant hay que escapar
  +  `<` de verdad**, no solo el cierre.
  +- Toda URL de metadata (canonical, `og:image`) es **ABSOLUTA** y sale de `APP_CONFIG.dominio`.
  +  Los crawlers sociales no resuelven rutas relativas ni rasterizan SVG: `og:image` apunta a un PNG
  +  1200×630 (`npm run og:png` lo regenera desde `public/og.svg`).
  +
  +## Copy de superficie de marca: invariantes testeados
  +
  +- El copy de la landing (`src/components/landing/copy.ts`) tiene **reglas de negocio y legales
  +  dentro del texto** (palabra prohibida, una sola mención del procesador, promesas económicas con
  +  su apellido). Se blindan con tests que leen los **exports aplanados** del módulo, no el archivo
  +  crudo — así sobreviven a una reescritura completa del copy. Ver
  +  `src/__tests__/components/landing-copy.test.ts`.
  +- **Antes de afirmar que el usuario VE algo, verificar la pantalla.** En esta task el copy vivo
  +  prometía que el Comprador "ve su número en pantalla y en su correo" y ninguna superficie lo
  +  muestra; el primer reemplazo introdujo la misma mentira sobre el panel. Una promesa de UI en el
  +  copy se paga en soporte: se verifica contra el componente/use case, no contra la intención.
  +- **Remate de sección con plumón**: cuando una banda de la landing cierra con una frase-promesa
  +  ("Todo esto, **en una tarde.**", "**Menos de mil pesos al día.**"), va en un `Text` más pesado
  +  que el cuerpo CON `Plumon` sobre la parte destacada. Sin el plumón, un remate con el mismo
  +  tratamiento tipográfico que los títulos de card se lee como un ítem huérfano de la grilla.
  ```
  Opciones para el usuario: (1) aplicar tal cual; (2) aplicar con ajustes; (3) dejar el draft acá y aplicarlo después a mano; (4) ignorar. **NO aplicado** — el implementer no toca `docs/agents/*` sin confirmación.
- [2026-07-26 00:35] [feature-implementer] **SUGERENCIAS que NO me corresponde aplicar** (son del `domain-planner` / del usuario): (a) `CONTEXT.md` no tiene entrada para el **ordinal del ticket como dato NO expuesto** — es justo la confusión que causó dos correcciones de copy en esta task; valdría una línea que diga que el `RaffleEntry.ordinal` existe en DB y hoy no se muestra a nadie; (b) D6 del plan («el gate de cobro del futuro billing = la PUBLICACIÓN de la tienda») quedó registrada como decisión heredable y ahora está IMPRESA en la landing ($25.000/mes, 2ª tienda a mitad de precio, cero comisión por venta) — amerita **ADR propio** antes de construir el billing, porque la landing ya la promete públicamente.
- [2026-07-26 00:40] [feature-implementer] **Implementación COMPLETA. `status: implementing → testing`.** F01..F05 escritas, 5 pasadas de `frontend-reviewer` (F01 APPROVE, F02 APPROVE, F03 REQUEST_CHANGES ×2 → APPROVE, F04 APPROVE, F05 APPROVE), 9 nits aplicados, 2 elevados. Vitest de la task **24/24** (filtrado: `landing-copy` 12, `landing-seo` 9, `config/app` 3); eslint limpio en `src/components/landing`; `tsc` sin errores propios. **NO corrí la suite completa ni `npm run check`** (rol del `feature-tester` / `change-set-reviewer`) — y ojo con el carril ajeno en vuelo del retiro del Operador, que hoy deja 45 errores de `tsc` que NO son de esta task. Sin commit (instrucción del usuario). E2E declarados en `tasks/e2e-landing-reposicionamiento.md`: 7 checks (`landing.hero.001`, `landing.bandas.001`, `landing.faq.001`, `landing.jsonld.001`, `landing.metadata.001`, `landing.robots.001`, `landing.og.001`). **Verificación visual en :3001 NO hecha** (paso 6 del Plan) — queda para el `feature-tester`/usuario.
- [2026-07-26 00:55] [feature-implementer] **Respuestas del usuario a las 5 preguntas — APLICADAS.** (1) **Número del comprador: SÍ debería verlo, es carril de producto FUTURO** (textual del usuario: *"más adelante haremos el buscador de tickets... o donde está tu cuenta mostrar los tickets"*). La landing NO promete nada de eso: se queda con lo verificable. **BACKLOG registrado acá y en `tasks/INDEX.md`** (no existe archivo de backlog en el repo): *mostrar el número/ticket al Comprador —en la pantalla de retorno y/o en el correo de descarga— + buscador de tickets*; cuando ese carril aterrice, hay que **borrar el test `landing.faq.005`** (lo dice su propio comentario) y reescribir la FAQ 5 y `CONFIANZA[1]`. (2) **Claims de comunidad corregidos a lo verificable**: `MOMENTO.texto` ya no dice «un talonario que toda tu comunidad puede ver» — ahora la fuerza está en el momento real (*aprietas sortear en pleno live y el ganador aparece al instante en tu pantalla*, que es lo que `ejecutarSorteo` sí hace y lo que el Organizador muestra en vivo); `TESTIMONIO.cita` ya no dice «y mi comunidad lo ve» (cambió a registro automático + correr el sorteo en vivo tranquila). `LOGIN.testimonio` no requería cambio (nunca tuvo el claim). (3) **Drift APLICADO tal cual** a `docs/agents/frontend-conventions.md`: 2 secciones nuevas — «Metadata SEO de la landing del apex» y «Copy de superficie de marca: invariantes testeados». (4) **`src/pages/login.tsx` corregido**: `og:image` pasa de `/og.svg` (relativo + SVG) a `URL_OG_IMAGEN` importado de `~/components/landing/seo` — fuente única, PNG absoluto. Es el ÚNICO cambio del login (copy y layout intactos, Out of scope respetado). (5) **`CTA_FINAL.eyebrow`: «Admite a 1 (una) tienda nueva» → «Admite a 1 (una) tienda»** — sacar «nueva» mata la lectura de cupo limitado y deja la grafía de boleto (ADMIT ONE), que además calza con el modelo real (un plan = una tienda; la segunda va a mitad de precio).
- [2026-07-26 04:05] [feature-tester] **Vitest de la task: 25/25 VERDE** (`landing-copy` 13, `landing-seo` 9, `config/app` 3; corrida filtrada `node node_modules/vitest/vitest.mjs run <3 archivos>` — ojo, `npx vitest` NO funciona hoy: `node_modules/.bin` no existe en este árbol). **19 checkboxes Vitest de Validaciones marcados `[x]`** (F01 ×5, F02 ×3, F03 ×5, F04 ×4, F05 ×2). Habilitado el E2E.
- [2026-07-26 04:05] [feature-tester] ⛔ **E2E BLOQUEADO — pre-condition del dev server no cumplida, por causa AJENA a esta task.** El server de :3001 está vivo pero devuelve **500 en toda ruta cubierta por el matcher del middleware**: `/`, `/login`, `/robots.txt`, `/sitemap.xml`, `/og.png` y `autora.localhost:3001`. Las rutas EXCLUIDAS del matcher (`/favicon.ico`, `/_next/static/*`) responden **200** ⇒ lo que revienta es el **edge middleware**, no las páginas ni el copy nuevo. Cuerpo de la respuesta: `Internal Server Error` pelado (firma de crash de edge, no de render de página).
- [2026-07-26 04:05] [feature-tester] Diagnóstico del 500 (todo read-only, no toqué nada): (a) `src/middleware.ts` **NO está modificado** — idéntico a HEAD `1920fec` — y su cadena de imports valida fuera del server (`node --env-file=.env` importando `src/env.js` ⇒ 21 keys, sin throw), así que ni el fuente ni el env explican el crash; (b) el árbol de herramientas SÍ está sucio: **`node_modules/.bin` y `node_modules/.package-lock.json` no existen** (npm install interrumpido — por eso `npx vitest` no resuelve) y `node_modules/.prisma/client` tiene `query_engine-windows.dll.node.tmp13120/.tmp21620/.tmp27800`, o sea un `prisma generate` abortado por EPERM — exactamente lo que el INDEX ya anticipaba para `builder-tanda-3` («restart dev server, generate EPERM por lock»); (c) `.next/server/app/` sigue existiendo aunque el carril `plataforma-retiro-operador` **borró `src/app/` entero**, y `.next/server/middleware-manifest.json` trae un matcher **stale** (`^/.*$`, `"/:path*"`) que no corresponde al del fuente ⇒ el dev server quedó con estado viejo de antes de esos borrados. Probé el nudge no destructivo (`touch src/middleware.ts`, cero bytes cambiados, `git status` limpio) y **no recompiló**: sigue 500. Remedio = **reiniciar el dev server** (una sola instancia, memoria del proyecto). NO lo hago sin autorización: el server es del usuario.
- [2026-07-26 04:05] [feature-tester] Lo que SÍ pude verificar sin server (no reemplaza el E2E, lo adelanta): **`public/og.png` leído a ojo** — PNG real 1200×630 (40.253 bytes), wordmark «Sortéatelo» con la «é» en amarillo sobre la «a» en tinta, titular «Tu tienda. Tu sorteo.» con el plumón **DEBAJO** del texto (el tachado que reportó el implementer está efectivamente corregido), tagline nuevo «Monta tu sorteo online tú mismo, en un día.» y chip mono «N° 000428», todo en fuentes de marca (nada de serif de sistema) ⇒ cubre la mitad visual de `landing.og.001`, falta el `GET 200 image/png`. **`public/robots.txt`** (`User-agent: *` + `Allow: /` + `Sitemap:` absoluto) y **`public/sitemap.xml`** (`<loc>https://sorteatelo.cl/</loc>`, sin `lastmod`) correctos en disco ⇒ falta el `GET 200` de `landing.robots.001`. **Nit de OG (no bloquea)**: el check `landing.og.001` pide «la «éa» en amarillo» y en el PNG la «é» es amarilla y la «a» es tinta — coincide con la gramática del wordmark, pero conviene alinear la redacción del check con el diseño real.
- [2026-07-26 04:05] [feature-tester] **E2E ejecutados: 0/9.** Ninguno de los 9 checks de `tasks/e2e-landing-reposicionamiento.md` queda marcado `[x]` NI marcado como fallado: sin HTML servido no hay evidencia para juzgarlos, y marcarlos ❌ atribuiría a esta task un fallo de infraestructura ajeno. AWAITING USER sobre el dev server.
- [2026-07-26 04:50] [feature-tester] **Dev server REINICIADO (opción 1 autorizada por el usuario) ⇒ el 500 murió.** `taskkill` del PID 46248 (`next dev` :3001), puerto libre, y relanzado `node node_modules/next/dist/bin/next dev -p 3001` en background — **una sola instancia** (memoria del proyecto). Ready en 1440 ms y post-restart **todo 200**: `/`, `/login`, `/robots.txt`, `/sitemap.xml`, `/og.png`, `autora.localhost:3001`. **Confirmado el diagnóstico**: era estado stale del dev server (`.next/server/app/` de un `src/app/` ya borrado + manifest de middleware viejo), causa AJENA a esta task. No hizo falta `npm install` ni `prisma generate`, así que no los corrí (el permiso quedó sin usar; `node_modules/.bin` sigue ausente — `npx` no funciona, hay que invocar `node node_modules/<pkg>/…`).
- [2026-07-26 04:50] [feature-tester] ⚠ **Hallazgo de HOST — la landing NO vive en `localhost:3001` en este dev.** `src/config.ts` tiene `devTienda.enabled: true` con slug `autora` (F09d, y es el estado **commiteado** del repo, no una edición local), así que el **apex pelado impersona la Tienda `autora`**: `curl localhost:3001` devuelve el storefront (`<title>Tienda de la Autora (piloto)</title>`), byte-idéntico a `autora.localhost:3001`. La landing de plataforma se alcanza en **`http://www.localhost:3001/`** — `parsearHost()` mapea el prefijo `www` a `{zona:"plataforma"}`, la MISMA zona que el apex, así que es equivalente y no exige tocar nada. **NO apagué `devTienda`** a propósito: hay otro `feature-tester` corriendo en paralelo (`plataforma-retiro-operador`) que está testeando sobre `localhost:3001`, y apagarlo le rompía la corrida. Nota registrada arriba en `tasks/e2e-landing-reposicionamiento.md` para las próximas corridas.
- [2026-07-26 04:50] [feature-tester] **E2E: 7 PASS + 2 PARCIAL de 9.** ✅ `landing.hero.001` (eyebrow, titular con `span.plumon` sobre «sorteo» y «Hoy», bajada, nota, **3** CTAs "Crea tu tienda gratis" → `/login`; `\brifa\b` = **0** y "Flow" = **1** en el texto visible). ✅ `landing.jsonld.001` (4 bloques `ld+json` en el orden Organization/WebSite/SoftwareApplication/FAQPage, los 4 parsean, `FAQPage` con 9 `Question` idénticas a las del Accordion, `Offer` 25000 CLP, sin `</script` inyectado). ✅ `landing.metadata.001` (canonical `https://sorteatelo.cl/`, `og:locale` es_CL, `og:image` PNG absoluto 1200×630, keywords con "rifa", title y description de 156 chars sin "rifa"; **control negativo del tenant limpio**: cero `ld+json`, cero canonical, cero "sorteatelo.cl" en su head). ✅ `landing.robots.001` (robots 200 `text/plain` 321 B con las 3 líneas; sitemap 200 `application/xml` con el `<loc>` absoluto). ✅ `landing.og.001` (200 `image/png`, 1200×630, **byte-idéntico** al de disco, leído a ojo). ✅ `landing.honestidad.001` (6 patrones de claim de visibilidad, todos AUSENTES; la banda del momento habla de la pantalla del Organizador; boleto sin "nueva"). ✅ `login.og.001` (og:image PNG absoluto, cero `/og.svg`). 🟡 `landing.bandas.001` y 🟡 `landing.faq.001` PARCIALES — detalle abajo.
- [2026-07-26 04:50] [feature-tester] Lo que cubrí de los 2 PARCIALES sin navegador, y lo que NO. **`landing.bandas.001`**: las **9 bandas salen en el orden D8 exacto** — verificado cruzando el orden del DOM servido con `landing.module.css`: `bandaAzul` → sin-modificador (BLANCA: `.banda` no declara fondo) → `bandaAmarilla` → BLANCA → `bandaAmarilla` → BLANCA → `bandaGris` → `bandaAzul` → `bandaTinta`, con **cero blancas adyacentes**; copy de precio completo; y el remate "Menos de mil pesos al día." es `span.plumon` DENTRO de `.bandaAmarilla`, donde la regla `.bandaAmarilla .plumon` conmuta a `plumon-blanco.svg` ⇒ **plumón blanco confirmado por stylesheet**. Falta solo el **viewport ~375px**. **`landing.faq.001`**: 9 `Accordion-item` + 9 `Accordion-control` en el orden aprobado + 9 `Accordion-panel` en el DOM SSR; respuesta 1 con "$25.000 al mes, IVA incluido" y "en definición" ausente de TODA la página; respuesta 4 única con "Flow". Falta solo **ejercer el click**. Motivo del residuo: **los dos carriles MCP de navegador estaban ocupados por agentes paralelos** (chrome-devtools PIDs 42320/39056, Playwright PIDs 21768/36264) y `browser-verify` §0/§5 prohíbe arrebatárselos; `claude-in-chrome` no tiene tools expuestos en mi contexto.
- [2026-07-26 04:50] [feature-tester] **Trampa para futuras corridas**: un `includes("rifa")` sin límite de palabra da **falso positivo** sobre esta landing — el texto dice "ta**rifa**" 2 veces (FAQ 1 y FAQ 4, "el procesador cobra su tarifa por transacción"). El barrido correcto es `\brifas?\b`, que da **0**. El test `landing.copy.001` ya usa límite de palabra; el que se equivocó primero fui yo al hacer el barrido del HTML.
- [2026-07-26 04:50] [feature-tester] **2 nits de diseño (ninguno bloquea)**: (a) el check `landing.og.001` pide «la «éa» en amarillo», pero en el PNG la «é» es amarilla y la «a» va en tinta — coincide con la gramática del wordmark, así que lo que conviene ajustar es el texto del check, no el arte; (b) la banda amarilla del momento incluye un bloque decorativo `role="img" aria-label="Ejemplo de talonario de sorteo"` que dibuja "Serie A · N° 301 M.P. · N° 302 CATA · N° 303…" — está marcado como ilustración *de ejemplo* y no afirma nada, pero es la versión VISUAL del claim que el usuario acaba de borrar del copy ("un talonario que toda tu comunidad puede ver"). Decisión de producto suya si lo revisa.
- [2026-07-26 04:50] [feature-tester] Log narrativo en **`tasks/.e2e-run-landing.log`** y NO en `tasks/.e2e-run.log`: ese archivo lo está usando en este momento el `feature-tester` de `plataforma-retiro-operador` (mi header inicial ya fue sobreescrito por el suyo). Dos agentes escribiendo el mismo log se pisan — vale la pena que el harness use un log por slug.
- [2026-07-26 05:15] [feature-tester] **E2E CERRADO 9/9.** El usuario confirmó que los agentes paralelos terminaron y la **sesión principal** ejerció los 2 residuos por el carril Playwright (el lock del perfil `mcp-chrome-12f6c4f` ya no tenía dueño vivo). **`landing.bandas.001` PASA**: a viewport **375×812**, `getComputedStyle` sobre las 10 bandas top-level devuelve AZUL `rgb(43,63,191)` (header) → AZUL (hero) → transparente/blanca → AMARILLA `rgb(255,197,48)` → blanca → AMARILLA (precio) → blanca → GRIS `rgb(238,240,245)` (FAQ) → AZUL (boleto) → TINTA `rgb(25,27,34)` (footer) ⇒ orden D8 exacto y cero blancas adyacentes **confirmados por color computado**, no solo por nombre de clase (que es lo que yo había podido verificar); sin desborde horizontal (`scrollWidth` ≤ `innerWidth` 376, bandas a 361px) y screenshot `tmp/landing-375-hero.png` con el plumón sobre «sorteo»/«Hoy», la bajada sin Flow y la nota "Gratis para partir · Sin tarjeta". **`landing.faq.001` PASA**: click real sobre el primer `.mantine-Accordion-control` («¿Cuánto cuesta?») ⇒ `aria-expanded` a `true`, panel visible con `offsetHeight` 237 y la respuesta abriendo **verbatim** con el modelo aprobado ("Configurar tu tienda es gratis: el plan corre cuando publicas. Son $25.000 al mes, IVA incluido…"); los 9 controls presentes.
- [2026-07-26 05:15] [feature-tester] **VEREDICTO FINAL: Vitest 25/25 + E2E 9/9, todo verde.** 19 checkboxes Vitest y 7 checkboxes E2E marcados `[x]` en Validaciones; los 9 checks de `tasks/e2e-landing-reposicionamiento.md` en `[x]`. Cero fallos funcionales. **NO toco `state` ni `status`** — los decide el usuario. Quedan 3 cosas anotadas, ninguna bloqueante: (a) **nit del check `landing.og.001`** — pide «la «éa» en amarillo» y el PNG tiene la «é» amarilla y la «a» en tinta; se ajusta el texto del check, no el arte; (b) **REVISABLE de producto** — la banda amarilla del momento lleva un bloque decorativo `role="img" aria-label="Ejemplo de talonario de sorteo"` con "Serie A · N° 301 M.P. · N° 302 CATA · N° 303…", que es la versión VISUAL del claim de visibilidad que el usuario acaba de borrar del copy; (c) **apunte de harness** — `tasks/.e2e-run.log` es un único archivo global y dos `feature-tester` en paralelo se lo pisan (el mío fue sobreescrito por el de `plataforma-retiro-operador`); mi log narrativo quedó en `tasks/.e2e-run-landing.log` y convendría un log por slug. Además: el dev server quedó **vivo y sano** en :3001 (PID nuevo, una sola instancia) — no lo bajo.
- [2026-07-26 00:55] [feature-implementer] Test nuevo `landing.copy.005` (anti urgencia/escasez, design.md §8) con su **límite documentado y verificado empíricamente**: atrapa el vocabulario típico («cupos limitados», «solo quedan», «últimas horas») pero **NO** atrapa la escasez implícita — comprobé que la frase que originó el fix, «Admite a 1 (una) tienda nueva», NO matchea ninguno de sus patrones. El comentario del test lo dice explícito para no dar falsa confianza: eso lo encontró una lectura humana, no la suite. Vitest de la task: **25/25**; `tsc` sin errores propios; eslint limpio en `src/components/landing`, `src/pages/login.tsx` y los tests. Sin commit. E2E: 2 checks nuevos en `tasks/e2e-landing-reposicionamiento.md` (`landing.honestidad.001` barrido de claims de visibilidad, `login.og.001`) ⇒ **9 checks** para el `feature-tester`.
