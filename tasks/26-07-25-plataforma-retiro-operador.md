---
slug: plataforma-retiro-operador
status: done
owner: nicolas
created: 2026-07-25
related_adrs: [ADR-0016, ADR-0019, ADR-0022]
related_context: [Operador, Tenant, TenantMembership, FlowCredential]

features:
  - id: F01
    behavior: "El MCP muere entero (/api/mcp + server/mcp + MCP_OPERADOR_TOKEN); los 3 helpers que consume el asistente IA se reubican en domain/pagebuilder y el asistente sigue verde"
    state: done

  - id: F02
    behavior: "Muere el borde backend del Operador: operadorProcedure, router operador (y su registro en root), src/server/domain/operador/ completo"
    state: done

  - id: F03
    behavior: "El editor visual autoriza SOLO por membresía: muere la rama god-mode de puedoEditar, exigirEditor, el procedure puedoEditar y getEditorProps"
    state: done

  - id: F04
    behavior: "authPolicy sin rol Operador: mueren esOperador/parsearAllowlist/emailEnLista, el flag esOperador de AccesoPanel/getAccesoActual y el param esOperador de resolverTenantAutorizado; PLATFORM_OPERATOR_EMAILS se borra del env schema y .env"
    state: done

  - id: F05
    behavior: "Mueren las superficies del panel (pagina /admin/operador, item rail, Spotlight, badge MenuCuenta, SinTiendaOperador) y el copy de suspension deja de nombrar al rol (los guards de SUSPENDIDA quedan intactos)"
    state: done

  - id: F06
    behavior: "Registro documental: ADR nuevo 'retiro del rol Operador de plataforma' (con el MCP futuro como decision DIFERIDA), addendums en ADR-0016/ADR-0022, limpieza de CONTEXT.md y backend-conventions.md"
    state: done
---

# Retiro completo del rol Operador de plataforma

## Contexto

El rol **Operador de plataforma** (allowlist `PLATFORM_OPERATOR_EMAILS`, god-mode cross-tienda) nació en F08 del roadmap como superficie de supervision (panel `/admin/operador` con suspender/reactivar) y se extendio con el editor MCP god-mode (`MCP_OPERADOR_TOKEN`, ADR-0016). El 2026-07-25, ADR-0022 D11 ya le nego el acceso cross-tienda en el panel de Organizador (`resolverTenantDelPanel` pasa `esOperador: false` hardcodeado): el rol quedo reducido a un borde propio que el usuario decidio **extirpar entero**. La supervision de plataforma sera un **superadmin futuro** (cosa aparte, D11) y el MCP renacera con otro modelo de auth (tokens per-usuario scoped a membresia + un MCP de plataforma aparte) — ambas cosas DIFERIDAS y documentadas, no construidas aqui.

Esto es un refactor de REMOCION: cero features nuevas, cero cambios de schema Prisma. `Tenant.estado = SUSPENDIDA` y sus guards quedan intactos (suspender = DB directa hasta el superadmin). Inventario completo verificado por grep en la Bitacora (arranque del grill + verificacion pre-plan): 78 archivos mencionan al rol; la mayoria son comentarios o factorias de test con `esOperador: false`.

## Decisiones

Todas cerradas con el usuario el 2026-07-25 (ver Bitacora Q1/Q2 + defaults confirmados):

- **D1 (alcance)**: extirpar el rol completo — panel, procedure, router, dominio, authPolicy, env, docs. Razon: tras D11/ADR-0022 el rol solo autorizaba su propio borde; mantenerlo es superficie de ataque y vocabulario muerto.
- **D2 (MCP — Q1 opcion c)**: el MCP actual muere ENTERO: `src/app/api/mcp/[transport]/route.ts`, las 12 tools (`src/server/mcp/`), `verificarBearer`, `MCP_OPERADOR_TOKEN` (env schema + `.env`). Razon: su auth es un token god-mode compartido, incompatible con el modelo futuro. El modelo futuro (tokens per-usuario scoped a membresia — "una persona tiene un token y ese token le permite configurar su tienda y tambien crear otra" — + un MCP de plataforma aparte para el usuario, junto al superadmin de D11) queda como decision **DIFERIDA fuera de alcance**, registrada en el ADR nuevo (F06).
- **D3 (snapshots)**: los `StorefrontPageVersion.publishedBy: "operador"` historicos NO se reescriben — el versionado es append-only. Solo muere la constante `PUBLICADOR_MCP` junto con las tools.
- **D4 (suspension — Q2 opcion a)**: el enum `SUSPENDIDA` y sus guards QUEDAN (el storefront niega tiendas suspendidas; `publicarTienda` sigue tirando `CONFLICT`). Solo mueren las superficies de suspender/reactivar. Suspension/reactivacion = DB directa hasta el superadmin futuro. Los copys que nombran al rol ("Contacta al Operador para reactivarla", checklist) se reescriben a neutro ("contacta al soporte de la plataforma" o equivalente).
- **D5 (authPolicy)**: `resolverTenantAutorizado` se simplifica — muere el param `esOperador` y sus dos ramas (seleccion god-mode + error INVALID "indica sobre que Tienda operar"). Muere `esOperador()`. `parsearAllowlist`/`emailEnLista` solo sobreviven con otro consumidor real: verificado que sus UNICOS consumidores son `trpc.ts` (operadorProcedure/AccesoPanel), `pagebuilder.ts` y `getEditorProps.ts` — todos mueren o se simplifican aqui ⇒ **mueren las tres funciones**.
- **D6 (editor)**: muere la rama god-mode de `puedoEditar` (param `esOperador`) y sus callers (`exigirEditor`, procedure `puedoEditar`, `getEditorProps`): editar una tienda = tener `TenantMembership`, punto. Fail-closed intacto.
- **D7 (preview)**: la preview tokenizada del pagebuilder (`STOREFRONT_PREVIEW_TOKEN`, `previewToken.ts`) **SOBREVIVE** — la usa el editor del panel, no el rol.
- **D8 (asistente IA — derivada mecanica de D2, verificada pre-plan)**: `src/server/domain/pagebuilder/asistente.ts` importa `outlineDe`, `mcpListWidgetTypes` y `mcpListStyleOptions` desde `~/server/mcp/tools`. Antes de borrar `server/mcp/`, esos 3 helpers se REUBICAN en `src/server/domain/pagebuilder/` (renombrados sin el prefijo `mcp`, p.ej. `outlineDe`/`listarTiposWidget`/`listarOpcionesEstilo`), con sus tests. El asistente es feature del panel y sobrevive intacto.
- **D9 (docs)**: ADR corto nuevo "retiro del rol Operador de plataforma" (registra D2-futuro y el superadmin como diferidos) + nota superseded/addendum en ADR-0016 (el MCP god-mode ya no existe) y ADR-0022 (la excepcion "el acceso cross-tienda sigue vivo en /admin/operador" ya no aplica) + limpieza de CONTEXT.md y `docs/agents/backend-conventions.md` (definicion del rol).
- **D10 (persona vs rol)**: lo que se retira es el ROL en codigo (allowlist + god-mode). El "Operador de plataforma" como PERSONA operativa (el freelancer que administra infra: cuenta R2, Cloudflare, deploy) sigue existiendo en CLAUDE.md/roadmap — los comentarios de infra que lo mencionan en ese sentido (p.ej. `env.js` sobre buckets R2) se dejan o se rebautizan minimamente, NO se barren a ciegas.

## Plan

1. **F01 — Matar el MCP.** (a) Reubicar `outlineDe`, `mcpListWidgetTypes`, `mcpListStyleOptions` de `src/server/mcp/tools.ts` a `src/server/domain/pagebuilder/` (D8) y actualizar el import de `asistente.ts`; migrar los tests de esos helpers desde `src/__tests__/server/mcp/tools.test.ts` a un test bajo `pagebuilder/`. (b) Borrar `src/app/api/mcp/` completo, `src/server/mcp/` completo, `src/__tests__/server/mcp/` completo. (c) Borrar `MCP_OPERADOR_TOKEN` de `src/env.js` (schema + runtimeEnv) y de `.env` (+ `.env.example` si existe). (d) Actualizar el literal `publicadoPor: "operador"` de `useCases.test.ts` a un valor neutro (p.ej. un email) y el docstring "MVP: operador" de `publicarPagina.ts`. (F01)
2. **F02 — Matar el borde backend del Operador.** Borrar `src/server/api/routers/operador.ts` + su registro en `root.ts`, `operadorProcedure` de `src/server/api/trpc.ts`, `src/server/domain/operador/` completo (listarTiendas/suspenderTienda/reactivarTienda/schemas), `src/__tests__/server/operador/` completo. (F02)
3. **F03 — Editor solo por membresia.** `puedoEditar` pierde el param `esOperador` y su early-return god-mode; `exigirEditor` y el procedure `puedoEditar` de `pagebuilder.ts` dejan de computar `esOp` (mueren sus imports de `esOperador`/`parsearAllowlist`); `getEditorProps.ts` idem. Ajustar `puedoEditar.test.ts`, `exigirEditor.test.ts`, `getEditorProps.test.ts` (las ramas god-mode se borran; las de membresia quedan). Comentarios de `banner-editar-tienda.tsx` y `accionSesion.ts` que citan "membresia/Operador" pasan a "membresia". (F03)
4. **F04 — Simplificar authPolicy + env.** En `src/server/authPolicy.ts`: borrar `esOperador()`, `parsearAllowlist`, `emailEnLista`; `AccesoPanel` pierde el campo `esOperador`; `resolverTenantAutorizado` pierde el param `esOperador` y sus ramas (queda: seleccion ⇒ membresia o FORBIDDEN; sin seleccion ⇒ primera membresia o FORBIDDEN); `resolverTenantDelPanel` deja de pasar `esOperador: false` y su docstring D11 se actualiza (la excepcion del panel Operador ya no existe). En `trpc.ts`, `panelProcedure` deja de computar el flag. `getAccesoActual.ts` pierde `esOperador` de su output. Borrar `PLATFORM_OPERATOR_EMAILS` de `src/env.js` (schema + runtimeEnv) y de `.env`. Sweep mecanico de tests: `authPolicy.test.ts` (mueren los describes de parsearAllowlist/emailEnLista/esOperador y las ramas Operador de resolverTenantAutorizado), `getAccesoActual.test.ts` (`panel.acceso.003` muere o se reescribe sin el flag), y TODAS las factorias de `AccesoPanel` con `esOperador:` en `src/__tests__/server/panel/`, `camposCheckout/`, `tenants/`, `correo/` (~30 archivos, cambio mecanico: borrar el campo). (F04)
5. **F05 — Matar las superficies del panel + copy de suspension.** Borrar `src/pages/admin/operador.tsx`. En `admin-layout.tsx`: item "Operador" del rail, entrada Spotlight, badge "Operador de plataforma" del MenuCuenta, componente `SinTiendaOperador` (el usuario sin tienda cae al empty state estandar), y el prop/plumbing `esOperador` (viene de `getAccesoActual`, ya muerto en F04 — F04 y F05 deben aterrizar juntas o F05 inmediatamente despues para que compile). Reescribir copy sin el rol: `publicarTienda.ts` (CONFLICT de SUSPENDIDA) y `checklist-publicacion.tsx` (aviso de tienda suspendida). Sweep final de comentarios que citan el ROL (auth.ts, guardAdmin.ts, guardPaginaAdmin.ts, empty-state.tsx, etc.), respetando D10 (los de la persona/infra quedan). (F05)
6. **F06 — Registro documental.** Escribir `docs/adr/` nuevo (numero siguiente disponible): "Retiro del rol Operador de plataforma" — contexto (D11 lo dejo sin proposito), decision (extirpacion total, suspension por DB directa), y las dos decisiones DIFERIDAS explicitas (superadmin futuro; MCP futuro con tokens per-usuario scoped a membresia + MCP de plataforma aparte). Addendum/nota superseded en ADR-0016 y ADR-0022. Limpiar la definicion del rol en `CONTEXT.md` y `docs/agents/backend-conventions.md` (dejando nota de que el termino "Operador de plataforma" sobrevive solo como persona operativa, D10). (F06)
7. Al cierre, verificacion global del implementer: `grep -ri "operador" src/` solo devuelve (a) copys/comentarios de la persona operativa permitidos por D10 y (b) nada compilable del rol; `vitest run` filtrado verde en las areas tocadas. El gate completo (`npm run check`) lo corre el `change-set-reviewer`.

## Validaciones

### F01 — MCP muerto, asistente vivo

**Vitest** (integration):
- [x] Los helpers reubicados (outline de pagina, listado de tipos de widget, listado de opciones de estilo) siguen cumpliendo su contrato desde `domain/pagebuilder` (tests migrados verdes). — `src/__tests__/server/pagebuilder/catalogoDelEditor.test.ts::page.catalogo.001` / `::page.catalogo.002` / `::page.catalogo.003` ✅ 2026-07-25 (3/3)
- [x] La suite del asistente IA sigue verde sin que exista `src/server/mcp/` (imports resueltos desde la nueva ubicacion). — `src/__tests__/server/pagebuilder/asistente.test.ts::page.ai.001-003` ✅ 2026-07-25 (3/3)
- [x] No queda referencia compilable a `~/server/mcp` ni a `MCP_OPERADOR_TOKEN` en `src/` (tsc verde + grep cero). — `npx tsc --noEmit` verde + `grep -rn "server/mcp\|MCP_OPERADOR_TOKEN" src/` cero ✅ 2026-07-25 (tsc exit 0 en todo el repo; grep = 2 lineas, ambas COMENTARIOS de arqueologia en `catalogoDelEditor.ts:37` y su test, cero compilable; `src/app/` y `src/server/mcp/` no existen en disco)

**E2E** (browser):
- [x] `POST /api/mcp` (cualquier transporte) responde 404 en el dev server. — `tasks/e2e-plataforma-retiro-operador.md#retiro.mcp.001` ✅ 2026-07-26 (404 en las 5 variantes probadas —`/api/mcp`, `/api/mcp/http`, `/api/mcp/sse`, en apex y subdominio, POST y GET, con y sin Bearer—; **cero 401** ⇒ no quedó handler vivo, y el cuerpo es la página 404 de Next, no un JSON-RPC de error)

### F02 — Borde backend del Operador muerto

**Vitest**:
- [x] El `appRouter` ya no expone la key `operador` (tsc: cualquier `api.operador.*` residual rompe compilacion; grep cero). — `npx tsc --noEmit` verde + `grep -rn "api\.operador\|operadorRouter\|domain/operador" src/` cero ✅ 2026-07-25 (grep 0 lineas, incluido `operadorProcedure`; `src/server/domain/operador/` y `src/pages/admin/operador.tsx` no existen)
- [x] La suite completa del area panel/tenants corre verde sin `operadorTiendas.test.ts` ni `src/server/domain/operador/`. — `npx vitest run src/__tests__/server/panel/ src/__tests__/server/tenants/` 153/153 ✅ 2026-07-25 (corrida ampliada: 82 archivos / 598 tests verde, 0 fallos)

**E2E**:
- [ ] (no aplica — backend-only; la superficie UI se valida en F05)

### F03 — Editor autoriza solo por membresia

**Vitest**:
- [x] `puedoEditar` devuelve `puedeEditar: true` sii existe `TenantMembership` (tenantId, userId); sin membresia ⇒ `false` — ya no existe ningun bypass por email/allowlist. — `src/__tests__/server/pagebuilder/puedoEditar.test.ts::page.editar.001` / `::page.editar.002` / `::page.editar.003` (el 003 es el guard de regresion nuevo: un flag de rol heredado en el input NO autoriza) ✅ 2026-07-25 (5/5; el guard "ni siquiera con un flag de rol heredado en el input" corre verde)
- [x] `exigirEditor` tira FORBIDDEN para un usuario logueado sin membresia en el tenant del host. — `src/__tests__/server/pagebuilder/exigirEditor.test.ts::page.editor.gate.002` ✅ 2026-07-25 (3/3: anonimo, miembro de OTRO tenant, y el caso feliz)
- [x] `getEditorProps` devuelve 404 neutral para un usuario logueado sin membresia (indistinguible de host sin tienda / sin sesion). — `src/__tests__/server/editor/getEditorProps.test.ts::page.editor.ssr.003` ✅ 2026-07-25 (5/5; el caso "sin permiso ⇒ notFound (no filtra previewToken)" verde junto a las otras 3 ramas neutrales)

**E2E**:
- [x] La dueña (con membresia) sigue pudiendo abrir `/editor` en el subdominio de su tienda; una cuenta sin membresia recibe 404. — `tasks/e2e-plataforma-retiro-operador.md#retiro.editor.001` ✅ 2026-07-26 (dueña en `autora.localhost:3001/editor` ⇒ **200** con el editor; la MISMA cuenta en `prueba.localhost:3001/editor` —tienda PUBLICADA donde no tiene membresia— ⇒ **404**; anonima ⇒ 404; host inexistente ⇒ 404. Los 3 cuerpos de 404 son **byte-identicos** (21.932 bytes, mismo sha1) ⇒ neutralidad real, y no filtran `previewToken|membres|operador`)

### F04 — authPolicy y env sin rol Operador

**Vitest**:
- [x] `resolverTenantAutorizado` con seleccion dentro de la membresia ⇒ la devuelve; seleccion fuera ⇒ FORBIDDEN; sin seleccion ⇒ primera membresia o FORBIDDEN — sin ninguna rama god-mode ni el error INVALID del Operador. — `src/__tests__/server/authPolicy.test.ts` (4 casos + el guard de regresion nuevo con cast) ✅ 2026-07-25 (7/7, los 4 casos + el guard "un flag de rol heredado en el input no autoriza nada" + los 2 de `resolverGuard`)
- [x] `resolverTenantDelPanel` conserva su semantica exacta (host sin tienda ⇒ FORBIDDEN; host fuera de membresia ⇒ FORBIDDEN) con la firma simplificada. — `src/__tests__/server/panel/tenantDelHost.test.ts::panel.host.001` / `::panel.host.002` / `::panel.host.004` ✅ 2026-07-25 (3/3)
- [x] `getAccesoActual` ya no expone `esOperador` y sus casos restantes siguen verdes (`panel.acceso.003` eliminado o reescrito). — `src/__tests__/server/panel/getAccesoActual.test.ts` (`panel.acceso.003` ELIMINADO: su sujeto era el campo que ya no existe; el escenario que arrastraba sigue cubierto por `::panel.acceso.007`) ✅ 2026-07-25 (7/7; el 007 "un host fuera de la membresia no es tienda activa para nadie" verde)
- [x] `AccesoPanel` sin el campo `esOperador` compila en todas las factorias de test del panel (sweep completo, tsc verde). — 28 factorias barridas + `node node_modules/typescript/bin/tsc --noEmit` sin errores propios ✅ 2026-07-25 (tsc exit 0 en TODO el repo, cero errores propios y cero ajenos)
- [x] El env schema ya no declara `PLATFORM_OPERATOR_EMAILS` ni `MCP_OPERADOR_TOKEN` y la app arranca sin ellas en `.env`. — `src/env.js` (schema + runtimeEnv), `.env` y `.env.example` limpios; `grep` cero ✅ 2026-07-25 (grep cero + verificacion de ARRANQUE real: importar `src/env.js` con el `.env` actual valida OK, y el bundle edge compilado del middleware —que importa `~/env`— evalua y responde 200 con CSP al ejecutarlo en el runtime edge de Next)

**E2E**:
- [ ] (no aplica — backend-only)

### F05 — Superficies del panel muertas + copy de suspension neutro

**Vitest**:
- [x] `publicarTienda` sobre tienda SUSPENDIDA sigue tirando CONFLICT (guard intacto) con el copy nuevo que no nombra al rol Operador. — `src/__tests__/server/tenants/publicarDespublicar.test.ts::tenants.publicacion.002d` ✅ 2026-07-25 (10/10 el archivo; el copy de `publicarTienda.ts:49` es "Tu tienda esta suspendida. Escribele al soporte de la plataforma para reactivarla." — cero mencion al rol)

**E2E**:
- [x] `/admin/operador` responde 404 en el subdominio y en el apex. — `tasks/e2e-plataforma-retiro-operador.md#retiro.panel.001` ✅ 2026-07-26 (**404 con sesion valida** —sin cookie la ruta redirige 307 al login y el 404 no seria observable— en los 4 hosts: apex real `www.localhost:3001`, apex pelado `localhost:3001`, y los subdominios `autora.` y `demo-editorial.`; control de sesion viva en la misma corrida: `/admin` ⇒ 200)
- [x] El rail del panel no muestra el item "Operador", el Spotlight (Cmd+K) no ofrece la entrada, y el MenuCuenta no muestra el badge "Operador de plataforma". — `tasks/e2e-plataforma-retiro-operador.md#retiro.panel.002` ✅ 2026-07-26 (**en navegador**: rail = 5 anchors exactos; Spotlight abierto con Ctrl+K real ⇒ `oper`/`operador`/`Operador`/`plataforma` los 4 en **`nothingFound` con 0 acciones**, con control positivo `prod` ⇒ Productos y query vacia ⇒ solo las 5 secciones; MenuCuenta abierto por click ⇒ nombre+email+"Cerrar sesion" y `[class*=Badge]` **vacio**; `body.innerText` sin `/operador/i`. Refuerzo: en los 8 chunks JS de `/admin`, `Operador de plataforma`/`SinTiendaOperador`/`esOperador`/`/admin/operador` = **0 hits**; las 2 unicas ocurrencias de "operador" son comentarios de `env.js` sobre la cuenta R2, D10)
- [x] Un usuario logueado sin tienda ve el empty state estandar (ya no existe `SinTiendaOperador`). — `tasks/e2e-plataforma-retiro-operador.md#retiro.panel.003` ✅ 2026-07-26 — **PRECISION del implementer**: lo que se ve es el **alta de Tienda** (`CrearTienda`), no un `EmptyState` con icono; es la unica pantalla de contenido del apex (ADR-0022/D1). El check E2E ya lo dice asi. (**En navegador** sobre `www.localhost:3001/admin` —el apex REAL de este entorno, ver nota de hosts— con un `User` **efimero de 0 membresias** creado y **borrado** en la misma corrida: se ve "Crea tu tienda" + el `<form>` con los 2 inputs y el submit "Crear mi tienda"; cero `/operador/i`. Ademas `panel.getAccesoActual` —el query que decide la rama en CLIENTE— devolvio `{"tenants":[],"tiendaActiva":null}` ⇒ `sinTienda === true`, **y su shape ya no trae `esOperador`**)
- [x] Una tienda con `estado = SUSPENDIDA` (toggle por DB directa, asistido) sigue con el storefront apagado y el checklist del panel muestra el aviso con el copy nuevo. — `tasks/e2e-plataforma-retiro-operador.md#retiro.suspension.001` ✅ 2026-07-26 (sobre `demo-editorial`, **estado previo PUBLICADA anotado y revertido al cerrar**: (a) storefront ⇒ 404 **byte-identico** al de un host inexistente, sin filtrar el motivo; (b) panel del miembro ⇒ 200; (c) el `Alert` renderiza el copy nuevo con `Anchor href="mailto:nikochaima72@gmail.com"` y el panel entero tiene **cero** `/operador/i`, sin boton Publicar; (d) `panel.publicarTienda` ⇒ **HTTP 409 CONFLICT** con el mensaje neutro y **sin cambiar el estado**)

### F06 — Registro documental

**Vitest**:
- [ ] (no aplica — solo docs)

**E2E**:
- [x] (no aplica — solo docs; validacion = checklist documental) ADR nuevo existe y registra las 2 decisiones diferidas; ADR-0016 y ADR-0022 tienen su nota superseded/addendum; CONTEXT.md y backend-conventions.md sin la definicion del rol. — `docs/adr/0023-retiro-del-rol-operador-de-plataforma.md` (NUEVO, con las 2 diferidas + 3 deudas conocidas); addendums en `docs/adr/0016-*.md` y `docs/adr/0022-*.md`; `CONTEXT.md` (4 entradas) y `docs/agents/backend-conventions.md` (6 bloques, incl. el shape de `AccesoPanel`); + `docs/ops/manual-puesta-en-produccion.html` y 4 xrefs colgantes en planes vecinos ✅ 2026-07-25 (verificado archivo por archivo: ADR-0023 existe y esta "aceptado" con la seccion de DIFERIDAS; 0016 y 0022 referencian el retiro; `CONTEXT.md:47-55` redefine el termino como persona operativa con el _Evitar_ explicito "el rol Operador (ya no existe en codigo)"; `backend-conventions.md:11` reescrito como "No hay rol de plataforma: la UNICA autorizacion es la membresia" con el puntero a los 2 guards de regresion)

## Invariantes

- I1: **Cero cambios de schema Prisma.** `Tenant.estado` (incl. `SUSPENDIDA`) y `StorefrontPageVersion` quedan intactos; nada de `db push`.
- I2: **Los guards de SUSPENDIDA quedan**: el storefront sigue negando tiendas suspendidas y `publicarTienda` sigue bloqueando con CONFLICT. Solo cambia el copy (D4).
- I3: **La preview tokenizada del pagebuilder sobrevive** (`STOREFRONT_PREVIEW_TOKEN`, `previewToken.ts`) — no tocarla (D7).
- I4: **Snapshots historicos append-only**: ningun `publishedBy` existente se reescribe (D3).
- I5: **Fail-closed sin sustituto**: al morir el god-mode, editar/operar una tienda ajena = FORBIDDEN/404 por membresia. Prohibido introducir cualquier bypass "temporal" (env var, email hardcodeado, flag).
- I6: **El asistente IA del editor no pierde capacidad**: los 3 helpers de D8 se reubican ANTES de borrar `server/mcp/`; su cobertura de tests migra con ellos.
- I7: **Tenancy intacta** (regla de oro): ninguna simplificacion de authPolicy puede aflojar el scoping por `tenantId` server-side ni la semantica de `resolverTenantDelPanel` (ADR-0022).
- I8: **Env limpio de verdad**: `PLATFORM_OPERATOR_EMAILS` y `MCP_OPERADOR_TOKEN` se BORRAN del schema de `env.js`, de `runtimeEnv` y de `.env` — no quedan declaradas inertes.
- I9: Ambiguedad fuera de estas decisiones/invariantes ⇒ parar y preguntar (p.ej. si aparece un consumidor no inventariado de `parsearAllowlist` o del MCP).

## Out of scope

- El **superadmin futuro** (D11/ADR-0022) — no se diseña ni se stubbed.
- El **MCP futuro** (tokens per-usuario scoped a membresia + MCP de plataforma) — solo se documenta como decision diferida en el ADR (D2).
- Cualquier **superficie nueva de suspension/reactivacion** — suspension = DB directa (D4).
- **Reescritura de snapshots** `publishedBy: "operador"` historicos (D3).
- Renombrar la **persona** "Operador de plataforma" en CLAUDE.md / roadmap / comentarios de infra (D10) — sobrevive como concepto operativo del freelancer.
- Cambios de schema, de flujo de pago, de storefront publico o del editor mas alla de la autorizacion.

## Especialistas a consultar

- `backend-reviewer` — revision de trpc.ts/authPolicy/routers tras la remocion (el area mas sensible: autorizacion fail-closed) y del env schema.
- `frontend-reviewer` — admin-layout.tsx (rail/Spotlight/MenuCuenta/empty state) y checklist-publicacion.tsx.
- `change-set-reviewer` — diff final + `npm run check` (el sweep toca ~40 archivos de test; el gate completo es obligatorio antes de commit).
- `feature-tester` — Vitest + E2E asistido (login Google real para F03/F05; toggle SUSPENDIDA por DB directa asistido por el usuario).
- (`schema-guardian` NO aplica — I1: cero cambios de schema.)

## Bitácora

- [2026-07-25] [planner-grill] Arranque. Inventario cargado (grep `operador|Operador|OPERATOR` en `src/` → 78 archivos). Señales estructurales confirmadas:
  - **Panel**: `src/pages/admin/operador.tsx`, item rail + Spotlight + badge "Operador de plataforma" + `SinTiendaOperador` en `src/components/admin/admin-layout.tsx`.
  - **Backend**: `operadorProcedure` en `src/server/api/trpc.ts`, router `src/server/api/routers/operador.ts` (registrado en `root.ts`), `src/server/domain/operador/` (listarTiendas / suspenderTienda / reactivarTienda / schemas).
  - **authPolicy** (`src/server/authPolicy.ts`): `esOperador()`, `parsearAllowlist()`, flag `esOperador` en `AccesoPanel`, rama Operador de `resolverTenantAutorizado` (tras D11/ADR-0022 su único consumidor `esOperador: true` real es el borde del Operador — al morir ese borde la rama queda muerta y la política se puede simplificar). `resolverTenantDelPanel` ya pasa `esOperador: false` hardcodeado.
  - **Env**: `PLATFORM_OPERATOR_EMAILS` en `src/env.js` + `.env`. Aparte (mecánicamente independiente): `MCP_OPERADOR_TOKEN` (Bearer del editor MCP god-mode, ADR-0016 — NO usa la allowlist de emails).
  - **Pagebuilder/MCP**: `src/server/mcp/{auth,tools}.ts`, `puedoEditar` (rama god-mode `esOperador`), `getEditorProps`, `banner-editar-tienda.tsx`, `accionSesion.ts`, `publishedBy: "operador"` en snapshots de StorefrontPageVersion.
  - **Suspensión**: `Tenant.estado = SUSPENDIDA` es real y con efecto de producto (apaga el storefront; `publicarTienda` bloquea con CONFLICT "solo el Operador reactiva"). El panel Operador es la ÚNICA superficie de suspender/reactivar.
  - **Tests**: `operadorTiendas.test.ts`, partes de `authPolicy.test.ts`, `getAccesoActual.test.ts` (`panel.acceso.003`), `publicarDespublicar.test.ts` (002d menciona al Operador), factorías con `esOperador` en ~70 tests de panel.
  - **Docs**: `docs/agents/backend-conventions.md` (definición del rol), `CONTEXT.md`, ADRs 0016/0019/0022 y otros que lo mencionan.
  - Contexto de decisión: ADR-0022 D11 (2026-07-25) ya rechazó el acceso Operador cross-tienda en el panel de Organizador; el superadmin del futuro será una cosa aparte.
- [2026-07-25] [planner-grill] Q1: ¿El editor MCP (god-mode por `MCP_OPERADOR_TOKEN`) sobrevive al retiro del rol, o muere también? Recomendada: sobrevive con renombre de vocabulario (token → `MCP_ADMIN_TOKEN` o similar, `publishedBy` nuevo → "mcp"; los valores históricos "operador" en versiones viejas se quedan como están).
- [2026-07-25] [planner-grill] Q1 answered: **(c) el MCP actual muere entero** (`/api/mcp` + tools + `MCP_OPERADOR_TOKEN`). Visión futura del usuario (FUERA de alcance, documentar junto al superadmin de D11): MCP con tokens per-usuario scoped a membresía (configurar su tienda, crear otra) + un MCP de plataforma aparte para él. Snapshots históricos `publishedBy: "operador"` NO se reescriben (append-only).
- [2026-07-25] [planner-grill] Q2: destino de suspender/reactivar (`Tenant.estado = SUSPENDIDA`) al morir su única superficie. Recomendada: (a) el enum y los guards quedan, las superficies mueren, suspensión por DB directa hasta el superadmin futuro. Defaults empaquetados en la misma ronda: borrar `PLATFORM_OPERATOR_EMAILS` de env schema y `.env`; simplificar `resolverTenantAutorizado` (muere el param `esOperador` y `esOperador()`; `parsearAllowlist`/`emailEnLista` solo si les queda otro consumidor); muere la rama god-mode de `puedoEditar`/`getEditorProps` (editar tiendas ajenas = por membresía); preview del pagebuilder SOBREVIVE (la usa el editor visual); ADR corto nuevo "retiro del rol Operador" + notas superseded en 0016/0022 + limpiar CONTEXT.md/backend-conventions.
- [2026-07-25] [planner-grill] Q2 answered (relevo del orchestrator): **(a) confirmada** + todos los defaults empaquetados confirmados por el usuario. Cierre del grill autorizado — escribir el plan sin re-preguntar.
- [2026-07-25] [planner-grill] Verificación pre-plan (greps dirigidos, sesión de cierre):
  - **Hallazgo clave (⇒ D8)**: `src/server/domain/pagebuilder/asistente.ts` (asistente IA, tanda 3) importa `outlineDe`, `mcpListWidgetTypes`, `mcpListStyleOptions` desde `~/server/mcp/tools` — borrar `server/mcp/` a secas rompe el asistente. El plan los reubica en `domain/pagebuilder/` antes del borrado.
  - Consumidores de `parsearAllowlist`/`emailEnLista`/`esOperador` confirmados: solo `trpc.ts` (AccesoPanel), `pagebuilder.ts` (exigirEditor + puedoEditar procedure), `getEditorProps.ts` — todos mueren/se simplifican aquí ⇒ las 3 funciones mueren (D5 resuelto).
  - `publicarPagina.publicadoPor` lo alimentan el router pagebuilder (email de la Organizadora — queda) y `PUBLICADOR_MCP` de tools.ts (muere). `useCases.test.ts:202` usa el literal `"operador"` — se neutraliza.
  - Copys de suspensión que nombran al rol: `publicarTienda.ts` (CONFLICT) y `checklist-publicacion.tsx` — se reescriben (D4); guards intactos.
  - `middleware.ts` y `csp.test.ts` NO referencian `/api/mcp` — cero trabajo ahí.
  - `admin-layout.tsx`: rail item (l.334-343), badge MenuCuenta (l.500-510), Spotlight (l.631-640), `SinTiendaOperador` (l.526-549, usado l.802-803) — todo mapeado.
- [2026-07-25] [planner-grill] **Plan escrito** (retomado por segundo planner tras interrupción). 6 features F01–F06, orden: MCP → borde backend → editor → authPolicy+env → superficies+copy → docs/ADR. Status queda `planning` a la espera del visto bueno del usuario al plan.
- [2026-07-25 22:35] [feature-implementer] Arranca implementación (visto bueno explícito del usuario al plan). Features pendientes: F01, F02, F03, F04, F05, F06. Read pass completo hecho (template, CLAUDE.md, ADR-0016/0022, backend-conventions, authPolicy.ts, mcp/*, asistente.ts, env.js, publicarPagina.ts). Working tree verificado: limpio salvo el carril `checkout-campos-configurables` (`exportarVentasCsv.ts`/`_csv.ts`/su test) — cordón respetado, no los toco.
- [2026-07-25 22:42] [feature-implementer] **F01 implementada** (MCP muerto, asistente vivo). TDD real 3 slices RED→GREEN sobre la reubicación (D8/I6): el test nuevo importaba del módulo inexistente ⇒ RED, se movió un helper por vez.
  - **Nuevo**: `src/server/domain/pagebuilder/catalogoDelEditor.ts` con los 3 helpers renombrados sin prefijo `mcp` — `outlineDe` (igual), `listarTiposWidget` (ex `mcpListWidgetTypes`), `listarOpcionesEstilo` (ex `mcpListStyleOptions`) + el helper privado `describir`. **Decisión táctica de naming/ubicación**: UN archivo, no tres, porque los tres comparten un único consumidor y un único propósito ("lo que el asistente de IA necesita LEER de la página sin ver el JSON crudo"); el nombre evita colisión con el widget `catalogo` (productos).
  - **Nuevo**: `src/__tests__/server/pagebuilder/catalogoDelEditor.test.ts` (3 tests, `page.catalogo.001-003`). El `003` es la migración literal de `mcp.style.003`; el `001`/`002` son cobertura NUEVA (antes `outlineDe` y `mcpListWidgetTypes` solo se ejercían de refilón desde las tools).
  - **Repuntado**: `asistente.ts` importa de la nueva ubicación (+ su docstring deja de citar al MCP). Suite del asistente 3/3 verde ⇒ I6 cumplido.
  - **Borrados**: `src/app/` COMPLETO (solo contenía `api/mcp/[transport]/route.ts`; el repo vuelve a ser 100% pages router), `src/server/mcp/` (auth.ts + tools.ts, incl. `verificarBearer` y la constante `PUBLICADOR_MCP`), `src/__tests__/server/mcp/`.
  - **Env (I8)**: `MCP_OPERADOR_TOKEN` fuera del schema Zod de `src/env.js`, de `runtimeEnv`, de `.env` y de `.env.example`. `STOREFRONT_PREVIEW_TOKEN` INTACTA (D7/I3) — solo se le quitó al comentario la mención al rol.
  - **Literales**: `useCases.test.ts` publica como `duena@tienda.cl` (era `"operador"`); docstrings de `publicarPagina.ts` sin el rol.
  - **D3 respetado**: los fixtures `publishedBy: "operador"` de `versionado.test.ts` se DEJAN — modelan filas históricas append-only, que es exactamente lo que D3 dice que no se reescribe.
  - Gate: `npx tsc --noEmit` **0 errores en todo el repo** (los 2 ajenos del carril de pago que anotaba el INDEX ya no están), `npx vitest run src/__tests__/server/pagebuilder/` **249/249 verde** (32 archivos).
  - **REVISABLE anotado, NO ejecutado**: la dependencia npm `mcp-handler` (+ el `@modelcontextprotocol/sdk` que arrastra) queda instalada y ya sin consumidores. El plan enumera qué borrar y `package.json` no está en esa lista ⇒ no la desinstalo por mi cuenta. Candidata a `npm uninstall mcp-handler` en el cierre.
- [2026-07-25 22:48] [feature-implementer] F01 — `backend-reviewer` **APPROVE, 0 blockers** (Compliance A / Naming A / Tests A / Documentación B). Verificó I3 (preview intacta en env+`previewToken.ts`), I6 (contrato exacto de los 3 helpers; la cobertura que murió con `tools.test.ts` era la del wrapper y su semántica ya vivía en `useCases.test.ts`/`estiloTema.test.ts` ⇒ sin hueco), I8 y D3. Confirmó que nada en config asume `src/app/` (`next.config.js` sin `appDir`; el `matcher` de `middleware.ts` nunca nombró `/api/mcp`) y que no hay riesgo cross-tenant nuevo. 4 nits, todos ya asignados: NIT-1 docstrings del repo que citan al MCP como vivo (routers/pagebuilder, domain/pagebuilder/*, lib/pagebuilder/*) ⇒ sweep F05/F06; NIT-3 `previewToken.ts:3` dice "para que el Operador revise" ⇒ sweep F05; NIT-4 `tasks/e2e-storefront.md` conserva `pagebuilder.mcp.001` histórico ⇒ nota en F06; NIT-2 `mcp-handler` huérfana en `package.json` ⇒ decisión del usuario al cierre (coincide con mi REVISABLE). **Nota operativa del reviewer**: el `.next` del dev server puede tener aún el manifiesto de la ruta app ⇒ el E2E `retiro.mcp.001` exige reiniciar el dev server antes de medir el 404.
- [2026-07-25 22:50] [feature-implementer] **F02 implementada** (borde backend del Operador muerto).
  - **DESVIACION DEL PLAN, verificada por grep — no hay decisión que tomar, solo un hecho**: `operadorProcedure` **NO EXISTE** como símbolo en el repo (el inventario del grill lo dio por existente). Lo real: `routers/operador.ts` usaba `panelProcedure` y la autorización vivía DENTRO de cada use case (`if (!acceso.esOperador) throw FORBIDDEN`). O sea el borde era el router + el gate del dominio, no un procedure propio. Lo único que quedaba de `operadorProcedure` eran menciones en docstrings (`trpc.ts`, `authPolicy.ts`, `guardAdmin.ts`) y en docs (ADR-0022, backend-conventions) — se barren en F04/F06. El efecto buscado por el plan se logra igual y completo.
  - **Borrados**: `src/server/api/routers/operador.ts`, `src/server/domain/operador/` completo (listarTiendas / suspenderTienda / reactivarTienda / schemas — verificado que nada fuera del módulo los importa), `src/__tests__/server/operador/`, y el registro `operador: operadorRouter` + su import en `root.ts`.
  - **Adelanto de ordenamiento (F05 → F02)**: `src/pages/admin/operador.tsx` se borró ACÁ y no en F05. Razón: era el ÚNICO consumidor de `api.operador.*`; dejarlo hubiera dejado el árbol sin compilar entre features, y el plan ya reconoce este acoplamiento de aterrizaje ("F04 y F05 deben aterrizar juntas o F05 inmediatamente despues para que compile"). Mismo archivo, mismo plan, una feature antes. El resto de F05 (rail/Spotlight/badge/`SinTiendaOperador`/copys) sigue en F05.
  - **Docstring**: `panelProcedure` en `trpc.ts` deja de describirse como "(Organizador / Operador)" y de prometer el `operadorProcedure`.
  - **D4/I2 respetados**: no se tocó `Tenant.estado` ni ningún guard de `SUSPENDIDA` — solo murieron las superficies de suspender/reactivar, que es exactamente lo que D4 manda. Suspensión = DB directa de acá en adelante.
  - Gate: `npx tsc --noEmit` **0 errores**; `npx vitest run src/__tests__/server/panel/ src/__tests__/server/tenants/` **153/153 verde** (27 archivos).
- [2026-07-25 22:56] [feature-implementer] F02 — `backend-reviewer` **APPROVE, 0 blockers** (Compliance A / Naming A / Tests A / Documentación B). **Confirmó y refinó el hallazgo**: `trpc.ts` tiene exactamente 4 procedures (`public`/`protected`/`tenant`/`panel`) y CERO middleware del rol; las menciones vivas a `operadorProcedure` en `src/` son **dos, no tres** (`authPolicy.ts:150` → F04, `guardAdmin.ts:33` → F05) más ADR-0022 y backend-conventions → F06. **I2 verificado en las dos puntas**: `resolverTenant.ts:90` sigue negando la tienda no PUBLICADA con respuesta neutral sin motivo, `resolverTenantAdmin` la sigue admitiendo en el panel, y `publicarTienda.ts:45-50` conserva el `CONFLICT` dentro de la `$tx` — cubierto por 4 specs independientes del módulo muerto. Verificó además que tras el borrado **ningún escritor de `Tenant.estado` entra ni sale de `SUSPENDIDA`** (`crearTienda`→CONFIGURACION, `publicarTienda`→PUBLICADA, `despublicarTienda`→CONFIGURACION), que es exactamente lo que D4 manda. Cero cobertura que reponer, cero dead code (`EstadoTiendaBadge` sigue usado por `admin/index.tsx`). Nits: **NIT-2 APLICADO YA** (el E2E `retiro.panel.001` decía F05; re-etiquetado a F02 con la razón, para que el tester no lo saltee); NIT-1 `backend-conventions.md:18` dice "6 páginas del panel" y ahora son 5 ⇒ **sumado al alcance de F06**; NIT-3 `slugTienda.ts:11` justifica los reservados con "el propio panel del Operador" ⇒ **solo el docstring** en F06, la entrada `operador` de la lista de slugs reservados se CONSERVA (sacarla liberaría un subdominio antes bloqueado = cambio de comportamiento fuera de alcance); NIT-4 `pages/dev/descargas.tsx:10` "página de OPERADOR local" cae bajo D10 (persona) ⇒ se deja.
- [2026-07-25 23:00] [feature-implementer] **F03 implementada** (editor solo por membresia). TDD real: el RED se escribio como GUARD DE REGRESION, no como test descartable.
  - **RED**: reescribi `page.editar.003` de "el Operador puede editar cualquier tienda" a "sin membresia no puede editar — **ni siquiera con un flag de rol heredado en el input**" (pasa `{esOperador:true}` via `as Parameters<typeof puedoEditar>[0]`). Contra el codigo viejo devolvia `puedeEditar: true` ⇒ rojo real. **GREEN**: murio el early-return god-mode. El test QUEDA: hace imposible reintroducir un bypass por flag sin que rompa, que es justo lo que pide I5.
  - `puedoEditar` pierde el param `esOperador` y su rama; su docstring explica que el bypass murio y por que la firma es la defensa.
  - Callers simplificados: `exigirEditor` y el procedure `puedoEditar` (`routers/pagebuilder.ts`) dejan de computar `esOp` ⇒ **los imports de `env` y de `esOperador`/`parsearAllowlist` quedaron huerfanos y se borraron del router** (el archivo ya no lee `env` en absoluto). `getEditorProps.ts` idem (conserva `env` porque sigue leyendo `STOREFRONT_PREVIEW_TOKEN`, D7/I3).
  - Comentarios al dia: `banner-editar-tienda.tsx`, `accionSesion.ts`, los 3 test files y el mock de `~/env` de `getEditorProps.test.ts` (ya no declara `PLATFORM_OPERATOR_EMAILS`).
  - Gate: `tsc --noEmit` **0 errores**; `puedoEditar.test.ts` **5/5**; panel+tenants **153/153**.
- [2026-07-25 23:02] [feature-implementer] **BLOQUEO DE ENTORNO (ajeno a esta feature, diagnosticado, NO resuelto).** Entre las 22:48 y las 22:55 el `node_modules` del repo quedo inconsistente por un proceso EXTERNO a mi sesion: desaparecieron `node_modules/.bin/` y `node_modules/.package-lock.json`, y el cliente Prisma generado quedo a medias (`.prisma/client/` tiene el DLL del engine + dos `query_engine-windows.dll.node.tmp*` huerfanos, pero le faltan `index.js`/`default.js`).
  - **Causa raiz**: `prisma generate` no puede completar el rename final del engine porque el **`next dev` de :3001 (PID 39964, hijo de 47344) tiene el DLL abierto** ⇒ `EPERM: operation not permitted, rename '...dll.node.tmp27800' -> '...dll.node'`. Es EXACTAMENTE el gotcha que el INDEX ya documento para el carril `builder-tanda-3` ("restart dev server (cliente Prisma nuevo, generate EPERM por lock)"). No hay salida sin detener ese proceso: el DLL esta mapeado en memoria, no se puede borrar ni sobreescribir.
  - **NO lo arregle por mi cuenta**: matar el dev server es una accion sobre infra COMPARTIDA del usuario y puede haber otro carril con una sesion de navegador viva contra :3001 (el prompt advirtio de carriles paralelos). Queda como pregunta al usuario.
  - **Radio de impacto real, medido**: 3 archivos de test no logran importar (`chromeTienda.test.ts`, `paginas.test.ts` — **que ni toque** — y `exigirEditor.test.ts`, que si es de F03). Todo lo demas corre: `tsc --noEmit` 0 errores (los tipos de Prisma si resuelven), panel+tenants 27 archivos/153 tests verde, pagebuilder 244 tests verde. O sea el bloqueo es de IMPORT en runtime, no de logica.
  - **Workaround usado para seguir**: invocar los binarios por ruta (`node node_modules/vitest/vitest.mjs`, `node node_modules/typescript/bin/tsc`) porque `npx` ya no los resuelve sin `.bin/`.
- [2026-07-25 23:12] [feature-implementer] F03 — `backend-reviewer` **APPROVE, 0 blockers** (Compliance A / Naming A / Tests A). Auditoria por lectura de los 3 bordes: enumero los procedures del router y confirmo que **18 de 20 pasan por `exigirEditor`**; los 2 que no son `puedoEditar` (booleano sobre uno mismo) y `asistenteDisponible` (flag global de env, sin dato de tenant, preexistente). Verifico que NO queda segunda puerta al documento (los use cases del pagebuilder solo los consume el router + `asistente.ts`, ya detras del gate; `src/app/` murio en F01 y `pages/api/` no expone nada del builder). Neutralidad de `getPropsEditor` intacta: mismo literal `{notFound:true}` en las 4 ramas Y corte antes de invocar la dependencia siguiente (anclado por `expect(mockAuth).not.toHaveBeenCalled()` / `expect(mockPuede).not.toHaveBeenCalled()`). Elogio explicito al estrechamiento `db: Pick<PrismaClient,"tenantMembership">`: hace **estructuralmente imposible** que el use case lea otra tabla para decidir. Sobre `page.editar.003` dictamino que **NO esta acoplado a implementacion** (assertea contrato publico + salida, no internals) y recomendo mantenerlo tal cual.
  - **Nits aplicados YA**: (a) el comentario del guard ahora dice **NO BORRAR EL CAST** y explica que su redundancia de hoy ES el punto — sin eso, un "limpiar ruido" futuro mataria el guard en silencio; (b) el E2E `retiro.editor.001` gano el aviso de correr contra tienda **PUBLICADA** (`getPropsEditor` arranca por `resolverBrandingSSR`, que solo resuelve PUBLICADA — corte PREEXISTENTE, el god-mode vivia un paso despues; sin la nota, un 404 legitimo se leeria como regresion de F03).
  - **Nits diferidos**: NIT-1 `revertirPagina.ts:9,46` sigue nombrando al Operador como actor del editor ⇒ sweep F05/F06 (no estaban en la lista previa); NIT-2 el router y `schemas.ts` citan al MCP como vivo ⇒ F06; NIT-3 `exigirEditor` usa copys distintos para anonimo vs sin-membresia — sin riesgo cross-tenant (distingue estado del PROPIO llamador), queda como opcion cosmetica.
  - **HALLAZGO A REGISTRAR EN EL ADR (F06)**: desde F03, el UNICO camino que queda para leer un borrador ajeno sin membresia es `getPropsHome` con `STOREFRONT_PREVIEW_TOKEN` — token unico de PLATAFORMA, sin sesion ni membresia, read-only. D7/I3 mandan conservarlo intacto y F03 no lo toco, pero deja de estar "tapado" por el god-mode: ahora es la superficie de acceso ancho que sobrevive. Un token per-tenant seria el cierre natural. Lo anoto como **deuda conocida** en el ADR nuevo, no lo cambio (fuera de alcance).
- [2026-07-25 23:15] [feature-implementer] **F04 implementada** (authPolicy sin rol + env limpio + sweep).
  - **TDD**: RED escrito como guard de regresion gemelo al de F03 — reescribi los 3 casos de Operador de `authPolicy.test.ts` en UNO que pasa `{esOperador:true}` por cast y exige **FORBIDDEN** en las dos ramas muertas (seleccion ajena, y sin membresia). Contra el codigo viejo devolvia `"X"` ⇒ rojo real. GREEN: `resolverTenantAutorizado` pierde el param y sus dos ramas.
  - **Mueren** `esOperador()`, `parsearAllowlist()`, `emailEnLista()` (D5 — verificado que no quedaba consumidor tras F03), el campo `esOperador` de `AccesoPanel`, su computo en `panelProcedure` (con el import de `~/env`, que `trpc.ts` ya no necesita) y el campo del output de `getAccesoActual`.
  - **`resolverTenantDelPanel` se simplifica de verdad**: ya no tiene que DECLARARLE `esOperador: false` a la politica para negar el atajo — el atajo no existe en ninguna parte, asi que no hay nada que declarar. El docstring lo dice asi.
  - **Env (I8)**: `PLATFORM_OPERATOR_EMAILS` fuera del schema Zod, de `runtimeEnv`, de `.env` y de `.env.example`. Con esto el repo NO tiene ninguna env var de rol.
  - **Sweep**: 28 factorias de `AccesoPanel` barridas mecanicamente (`panel/`, `camposCheckout/`, `tenants/`, `correo/`) + 3 semanticos a mano. **2 tests murieron con su sujeto**, no por conveniencia: `panel.acceso.003` ("expone esOperador=true") y `panel.host.003` ("el Operador con host ajeno tira FORBIDDEN") — el primero afirmaba un campo que ya no existe; el segundo esta SUBSUMIDO por `panel.host.002` (host ajeno ⇒ FORBIDDEN para cualquiera), que es la misma garantia sin el rol. Ninguna cobertura de tenancy se perdio.
  - **Referencia colgante corregida**: `guardAdmin.test.ts` apuntaba a `tenantDelHost.test.ts::panel.host.003`, que borre ⇒ repunta a `panel.host.002`.
  - **Cordon con el carril de checkout**: `exportarVentasCsv.test.ts` es de `checkout-campos-configurables` (en vuelo). Tuve que tocarlo igual porque su factoria de `AccesoPanel` rompe tsc si le queda el campo; mi diff ahi es **una sola linea** (`-  esOperador: false,`), dentro de lo que el plan declara para F04. No toque su logica.
  - Gate: `vitest` panel+tenants+camposCheckout+correo+authPolicy **38 archivos / 212 tests verde**.
- [2026-07-25 23:16] [feature-implementer] **F05 implementada** (superficies del panel + copy de suspension).
  - **`admin-layout.tsx`**: fuera el item "Operador" del rail, la entrada del Spotlight, el badge "Operador de plataforma" del MenuCuenta, el componente `SinTiendaOperador` entero, el prop `esOperador` de `NavbarContent`/`MenuCuenta` y el `const esOperador` que leia el acceso. El usuario sin tienda ahora cae SIEMPRE al alta (`CrearTienda`). Quedaron 5 imports huerfanos (`Badge`, `ThemeIcon`, `IconArrowRight`, `IconBuildingStore`, `IconShieldLock`) y se borraron.
  - **Copy de suspension neutro (D4), guards INTACTOS (I2)**: `publicarTienda.ts` sigue tirando el MISMO `CONFLICT` sobre `SUSPENDIDA`, solo cambia el texto a "Contacta al soporte de la plataforma para reactivarla"; `checklist-publicacion.tsx` idem ("Tu tienda fue suspendida. Escribele al soporte de la plataforma"). Cero cambios de logica en la maquina de estados.
  - **Sweep final de comentarios del ROL** (respetando D10): `auth.ts`, `guardAdmin.ts` (el bloque D11 se reescribio sin el rol y registra que se retiro entero), `guardPaginaAdmin.ts`, `empty-state.tsx`, `previewToken.ts` y `revertirPagina.ts` (los 2 que sumo el reviewer de F03), `schemas.ts`, `listarProductosDelPanel.ts`, `crearTienda.ts`, + 6 test files.
  - **`slugTienda.ts`: la entrada `"operador"` de los slugs reservados SE CONSERVA** (NIT-3 del reviewer de F02) con un comentario nuevo que explica por que: sacarla LIBERARIA un subdominio hasta hoy bloqueado, y eso es un cambio de comportamiento, no una limpieza. Solo se neutralizo el docstring.
  - **Se DEJAN a proposito** (D10 = persona operativa, no rol): las menciones de infra R2 en `env.js`, `storage.ts`, `storagePublico.ts`, `storagePublicoDeEnv.ts` y `pages/dev/descargas.tsx`. Y (D3) los fixtures `publishedBy: "operador"` de `versionado.test.ts` y `storefrontPage.test.ts`, que modelan filas historicas.
  - **Auto-chequeo `docs/design.md`**: F05 solo QUITA elementos del chrome — cero tokens, cero hex, cero color nuevo, cero monto, cero motion. No hay superficie visual nueva que calzar contra la linea grafica.
  - Gate: `vitest` panel+tenants+authPolicy+tenancy **34 archivos / 207 tests verde**. tsc: **0 errores mios**; el unico error del repo es AJENO y en vivo del carril `checkout-campos-configurables` (`exportarVentasCsv.ts:206` `rows` implicit any — su TDD en rojo; minutos antes el error era otro en el mismo modulo, o sea estan editando ahora mismo).
- [2026-07-25 23:24] [feature-implementer] F04 - `backend-reviewer` **APPROVE, 0 blockers** (Compliance A / Naming A / Tests A / Documentacion B). Audito I7 **caso por caso contra la spec historica**, no de memoria: contrasto los 4 casos sobrevivientes contra `backend-conventions.md:11` y las validaciones del plan que creo la politica (`26-07-16-panel-auth-organizadores.md:183-185`), confirmando que el `INVALID` era EXCLUSIVO del Operador y que **las dos ramas muertas eran alcanzables solo con `esOperador: true`** => cero cambio de semantica para un Organizador. Coincidio en que los 2 tests borrados no perdieron garantia (`panel.acceso.007` y `panel.host.002` cubren los escenarios residuales) y aporto el argumento de exhaustividad del sweep: una factoria con `true` que sostuviera un ALLOW estaria en rojo hoy => no quedo ninguna en silencio. Verifico ademas que `publicarDespublicar.test.ts:21` conserva `tenantIdDelHost: tenantIds[0] ?? "AJENO"`, o sea que `tenants.publicacion.006` sigue fallando por host ajeno y no por host nulo (load-bearing, sobrevivio). I8 limpio en las 3 puntas.
  - **NIT-1 (deuda, NO tocar en F04)**: la rama "sin seleccion => primera membresia" de `resolverTenantAutorizado` quedo **viva pero inalcanzable en produccion** - su unico llamador (`resolverTenantDelPanel`) siempre pasa el host. Es el mismo fallback que ADR-0022 mato en el borde. Borrarla seria cambio de semantica (I7 lo prohibe aqui) => **registrada como deuda en ADR-0023**, con el candidato natural (`tenantIdSolicitado` requerido).
  - NIT-2/NIT-3 (xrefs colgantes + runbook de produccion) - APLICADOS en F06.
- [2026-07-25 23:24] [feature-implementer] F05 - `frontend-reviewer` **APPROVE, 0 blockers, 5 nits**. Verifico el arbol de render **contra el guard, no contra el layout**: la rama `sinTienda` es alcanzable SOLO en el estado que `guardAdmin.ts:103` llama `alta` (apex + sesion + cero membresias), porque todos los demas casos se resuelven server-side antes de renderizar => `CrearTienda` es la unica salida correcta, y `SinTiendaOperador` colgaba DENTRO de la misma condicion, no la ampliaba. Confirmo que el `Divider` del pie no queda huerfano (`NAV_CONFIG` siempre trae Configuracion), que el rail colapsado y su `Tooltip` estan intactos, y que `NAV` sigue 1:1 con las **5** paginas de `src/pages/admin/`. Reviso los ~35 imports restantes uno por uno: todos con uso vivo. Valido el `color="red"` del Alert contra `ESTADO_TIENDA_COLOR.SUSPENDIDA` del theme (es token de estado, no decision suelta) y dio verde a mi auto-chequeo de `docs/design.md` (el motion del rail y su gate de `prefers-reduced-motion` quedaron intactos). **Cero drift de conventions**: F05 no establecio ningun patron nuevo, es remocion pura.
  - **Nits APLICADOS**: (a) `admin-layout.tsx:296` decia "Utilidades ancladas abajo (ajustes / **rol**)" y el "/ rol" apuntaba al item borrado => "(editor / ajustes)"; (b) `empty-state.tsx:9` tenia la cita mutilada `§tono` (unica ocurrencia del repo) => `docs/design.md §8, direccion «El Talonario»`; (c) **copy del Alert reescrito**: la 1a linea repetia el `title` sin informar nada, ahora lleva la CONSECUENCIA ("Mientras este suspendida no aparece publicada y no puede vender") + el camino, per §8; (d) **verbo unificado** entre el Alert del panel y el `CONFLICT` de `publicarTienda.ts` (los dos "Escribele al soporte de la plataforma"); (e) el E2E `retiro.panel.003` decia "empty state estandar" cuando lo que se ve es el **alta** => reescrito con aviso explicito para que el tester no busque un `EmptyState` con icono, y la Validacion del plan anotada con la precision (sin reescribir el texto del planner).
  - **Follow-up para el usuario (NO aplicado - seria superficie nueva)**: "el soporte de la plataforma" **no tiene canal** en el producto (grep de `mailto|soporte|contacto` en `src/`: cero). El copy manda a una puerta que no existe. No es regresion (el copy viejo mandaba a un rol igual de inalcanzable), pero el cierre natural es `APP_CONFIG.soporte` + `mailto:` en ese Alert.
  - **Drift pre-existente ANOTADO, no tocado** (el reviewer dijo explicitamente "no lo arregles aca"): `checklist-publicacion.tsx:231,244,424` sigue usando `Card withBorder` contra la regla de § Chrome del panel; el archivo ya migro parcialmente a `PanelCard`. Es carril de limpieza aparte.
- [2026-07-25 23:27] [feature-implementer] **F06 implementada** (registro documental) + **cierre del bloqueo de entorno**.
  - **ADR-0023 NUEVO** (`docs/adr/0023-retiro-del-rol-operador-de-plataforma.md`): contexto (D11 lo dejo sin proposito), la decision de una sola puerta (membresia), por que el MCP muere entero en vez de renombrarse, la suspension por DB directa, el append-only de los snapshots y la distincion persona/rol. Seccion propia para las **2 decisiones DIFERIDAS** (superadmin como superficie aparte; MCP con tokens per-usuario scopeados a membresia + MCP de plataforma), 5 alternativas descartadas con su razon, y **3 deudas conocidas que el retiro DESNUDA** (no crea): `STOREFRONT_PREVIEW_TOKEN` es ahora el unico camino a un borrador ajeno sin membresia (hallazgo del reviewer de F03); la rama muerta de `resolverTenantAutorizado` (NIT-1 del reviewer de F04); y `mcp-handler` sin consumidores.
  - **Addendums**: ADR-0016 (el documento JSON y el registro Zod siguen VIGENTES - lo que murio es uno de sus *editores*; la preview tokenizada sobrevive) y ADR-0022 (la excepcion "su acceso cross-tienda sigue vivo en /admin/operador" y "la politica conserva su rama de Operador" **ya no aplican**; la decision no cambia, se REFUERZA).
  - **`CONTEXT.md`** (4 entradas): "Operador de plataforma" reescrito como **persona operativa, NO sujeto autorizado**; el ciclo de vida aclara que suspender es DB directa; "Borrador" ya no dice "Operador via MCP"; "Editor MCP" marcado **(RETIRADO)** con su reemplazo diferido. Vocabulario NUEVO: cero - solo se corrigio lo que quedo falso.
  - **`docs/agents/backend-conventions.md`** (6 bloques): encabezado, quien loguea, el fail-closed, el bullet del rol reescrito como "no hay rol de plataforma" (con la prohibicion explicita de reintroducir bypass y el puntero a los 2 guards de regresion), "6 paginas" -> **5**, la excepcion de router propio, y - lo que el reviewer marco como **lo mas urgente del paquete** - la fila de `panelProcedure`, que documentaba el shape `AccesoPanel = { userId, email, esOperador, ... }`: un implementer futuro habria escrito codigo contra un tipo inexistente.
  - **Fuera de la enumeracion original del plan, sumado por los reviewers**: `docs/ops/manual-puesta-en-produccion.html` (su tabla mandaba a setear en prod una env var que ya no existe) + **4 xrefs colgantes** en planes vecinos, anotadas SIN reescribir su historia (`26-07-25-admin-multi-tienda.md` x2, `e2e-admin-multi-tienda.md#admin.multitienda.007` y `e2e-storefront.md#pagebuilder.mcp.001`, los dos ultimos marcados **NO RE-EJECUTAR** con la razon y el check que los reemplaza).
  - **BLOQUEO DE ENTORNO RESUELTO SOLO** (~23:26): el cliente Prisma volvio a estar completo (`.prisma/client/index.js` y `default.js` presentes) - el carril vecino evidentemente reinicio su dev server y el `generate` pudo completar el rename. Los 3 archivos que no importaban ahora corren, y en particular **`exigirEditor.test.ts` 3/3 VERDE**: la unica validacion de F03 que habia quedado sin correr esta cubierta. Quite el aviso de esa Validacion.
  - **Gate final**: `node node_modules/typescript/bin/tsc --noEmit` **0 errores en todo el repo** (el error ajeno del carril de checkout tambien se resolvio de su lado) + `vitest` filtrado sobre panel/tenants/pagebuilder/editor/tenancy/authPolicy/camposCheckout: **76 archivos / 506 tests VERDE**.
  - **Verificacion global del plan (paso 7)**: `grep -ri "operador" src/` => **35 lineas, cero simbolos vivos**. Las 3 cubetas legitimas: (a) persona/infra R2 permitida por D10; (b) arqueologia deliberada en `authPolicy.ts`/`puedoEditar.ts`/`guardAdmin.ts`/`slugTienda.ts`, redactada en pasado ("hasta el retiro..."); (c) fixtures historicos `publishedBy: "operador"` (D3). `src/app/`, `src/server/mcp/`, `src/server/domain/operador/` y `src/pages/admin/operador.tsx` no existen.
- [2026-07-25 23:28] [feature-implementer] **Implementacion completa.** F01..F06 escritas; 5 reviewers invocados y **TODOS APPROVE con 0 blockers** (`backend-reviewer` x4 en F01/F02/F03/F04, `frontend-reviewer` x1 en F05). Plan a `status: testing`, listo para `feature-tester`. **Sin commit** (no se pidio). Pendientes de decision del usuario en el mensaje de cierre: la dependencia `mcp-handler` huerfana, el canal de soporte inexistente que el copy nuevo menciona, y el drift pre-existente de `Card withBorder`.
- [2026-07-26 00:05] [feature-tester] **Vitest VERDE, E2E BLOQUEADO por el dev server.** Corrida sobre el area de la feature (`pagebuilder/`, `panel/`, `tenants/`, `editor/`, `authPolicy`, `camposCheckout/`, `correo/`, `tenancy/`, `styles/`): **82 archivos / 598 tests, 0 fallos**. `node node_modules/typescript/bin/tsc --noEmit` **exit 0 en todo el repo** (ni propios ni ajenos — el carril `landing-reposicionamiento` esta editando en vivo pero compila). Entorno: `node_modules/.bin/` sigue VACIO ⇒ vitest y tsc invocados por ruta (`node node_modules/vitest/vitest.mjs`), como aviso el implementer.
  - **13 checkboxes de Vitest + el checklist documental de F06 marcados `[x]`**, cada uno con su evidencia. Verificados uno por uno los IDs criticos: `catalogoDelEditor` 3/3, `asistente` 3/3, `puedoEditar` **5/5 incl. el guard "ni siquiera con un flag de rol heredado en el input"**, `exigirEditor` 3/3, `getEditorProps` 5/5, `authPolicy` **7/7 incl. el guard gemelo**, `tenantDelHost` 3/3, `getAccesoActual` 7/7, `publicarDespublicar` 10/10 (incl. `SUSPENDIDA ⇒ CONFLICT`).
  - **Greps del paso 7 re-corridos, todos cero simbolos vivos**: `api.operador|operadorRouter|domain/operador|operadorProcedure` = **0 lineas**; `server/mcp|MCP_OPERADOR_TOKEN|PLATFORM_OPERATOR_EMAILS` = 2 lineas, ambas COMENTARIOS; `esOperador|parsearAllowlist|emailEnLista` = solo comentarios de arqueologia + los 2 guards de regresion que pasan el flag por cast para probar que NO autoriza. `grep -i operador src/components/admin/` = **cero**. En disco: `src/app/`, `src/server/mcp/`, `src/server/domain/operador/` y `src/pages/admin/operador.tsx` **no existen**.
  - **I8 verificado en ARRANQUE, no solo por grep**: importar `src/env.js` con el `.env` actual valida OK, y el bundle **edge compilado del middleware** (que importa `~/env`) evalua y devuelve **200 con el header CSP** al ejecutarlo a mano en el runtime edge de Next. O sea: la app arranca sin las 2 env vars borradas.
  - **BLOQUEO E2E (ajeno al codigo de la feature, diagnosticado a fondo)**: el `next dev` de :3001 (PID 47344/39964, el mismo del bloqueo de las 23:02) responde **500 "Internal Server Error" en texto plano a TODA ruta dinamica** — apex, subdominios, `/api/auth/csrf` e incluso `/_next/webpack-hmr` —, mientras que lo excluido del matcher del middleware (`/_next/static/*`, `/favicon.ico`) responde **200**. O sea: **el middleware truena en cada request**. Descartado que sea codigo: (a) tsc 0 errores; (b) el `.env` valida contra el schema actual; (c) **el bundle edge que el server tiene en `.next/server/src/middleware.js` lo corri yo en el `EdgeRuntime` de Next y devolvio 200 + CSP**; (d) webpack sigue compilando bien (el server emitio `pages/index.js`, `admin/ventas.js` y `admin/configuracion.js` a las 23:39, durante mis requests). Hipotesis mas probable: el **contexto del sandbox edge quedo envenenado** — se creo a las 23:07:50, exactamente el mismo segundo en que F04 reescribio `.env` para sacar `PLATFORM_OPERATOR_EMAILS`; ese contexto congela `process.env` al crearse y solo se invalida si cambia un archivo del grafo del middleware (que no cambio desde entonces) o si se reinicia el server. Encaja con todo lo observado y con el aviso del `backend-reviewer` de F01 ("el E2E `retiro.mcp.001` exige reiniciar el dev server"). **Leccion operativa a registrar: tocar `.env` con `next dev` arriba deja el middleware muerto hasta el restart.**
  - **NO reinicie el server ni corri `npm install`** (ambas cosas prohibidas por la consigna, y hay carriles paralelos vivos). **NO toque la DB**: las 6 tiendas quedan en su estado original (todas PUBLICADA) — el toggle a SUSPENDIDA sin poder observar el efecto habria sido riesgo puro. DB y sesiones listas para cuando se desbloquee: hay 5 `Session` vigentes de `nikochaima72@gmail.com` (la mas nueva expira 2026-08-25) y `demo-editorial` tiene 1 membresia.
  - **Los 6 checks E2E quedan `[ ]`** con nota `⏸ NO MEDIDO` + la evidencia estatica que si pude reunir para cada uno. Ninguno fallo: no se midieron.
  - **Cambio extra de la sesion principal validado**: `APP_CONFIG.soporteEmail` existe en `src/config/app.ts` (client-safe, sin imports de `~/server`, con docstring que lo declara fuente unica y anota la tarea abierta del canal serio) y `checklist-publicacion.tsx:350-356` lo consume en un `Anchor href={mailto:...} underline="always"` dentro del `Alert` de suspension. El copy cierra el follow-up que el `frontend-reviewer` de F05 habia dejado abierto ("el soporte de la plataforma no tiene canal"). Verbo unificado con el `CONFLICT` de `publicarTienda.ts:49`. Design: `Alert color="red"` = token de estado `SUSPENDIDA`, correcto por `docs/design.md` (rojo para estado destructivo/error); cero montos, cero motion, cero hex ⇒ **0 design findings**.
- [2026-07-26] [feature-tester] **E2E DESBLOQUEADO Y VERDE: 6/6 checks medidos, 0 fallos, 0 auto-retries.** Segunda corrida sobre el mismo plan, con el entorno ya reparado por la sesion principal (`npm install` completo + dev server REINICIADO en :3001). Precondition re-verificada antes de empezar: apex 200, `autora.localhost:3001` 200, `/admin` sin sesion 307 al login ⇒ **el middleware volvio a vivir**, confirmando el diagnostico de las 00:05 (contexto edge envenenado al reescribir `.env` con el server arriba; se cura con restart, no con codigo). NO se reinicio nada: una sola instancia, la del usuario.
  - **CORRECCION DE ENTORNO IMPORTANTE (el aviso del archivo E2E estaba equivocado y hacia perder la corrida)**: el checklist mandaba a `lvh.me:3001` "porque la cookie `Domain=.lvh.me` cruza subdominios". En ESTE entorno `lvh.me` devuelve **404 a todo**: `.env` no define `NEXT_PUBLIC_PLATFORM_DOMAIN`, asi que `resolverConfigPlataforma` cae al default de dev `localhost` y `parsearHost` rechaza `lvh.me` fail-closed (no es el apex ni cuelga de el). Los hosts reales son `<slug>.localhost:3001`. Y hay una segunda trampa: `src/config.ts` tiene `devTienda.enabled = true` con slug `autora`, o sea **el apex pelado `localhost:3001` ES la tienda `autora`**, no la plataforma. El apex de verdad se alcanza por **`www.localhost:3001`** — la rama `prefijo === "www"` de `parsearHost` devuelve `zona: "plataforma"` ANTES del override, asi que da acceso al apex **sin tocar `src/config.ts`** (que ademas esta fuera de mi scope de edicion). Documentado como tabla de hosts en el encabezado del archivo E2E para que la proxima corrida no lo re-descubra.
  - **Sesion sin re-login manual**: la cookie de dev es host-only (`domain: undefined` sobre `localhost`, `auth.ts:85`), asi que se seteo **por host** con el `sessionToken` de la `Session` vigente mas nueva de la DB (metodo que documenta la Bitacora de `26-07-25-admin-multi-tienda.md`). Cero intervencion del usuario, cero sesiones invalidadas.
  - **Carriles de navegador: LOS DOS OCUPADOS, no le robe el navegador a nadie.** `chrome-devtools` respondio `browser is already running for ...chrome-devtools-mcp/chrome-profile` y Playwright `Browser is already in use for ...mcp-chrome-12f6c4f`; ambos con procesos `chrome.exe` VIVOS (verificado por `Win32_Process`: 20 procesos del perfil de chrome-devtools desde las 12:31 y 8 del de Playwright desde las 20:34). Per `browser-verify` §0/§5, matar uno de esos habria sido robarle la sesion a un agente en vuelo. **Solucion sin conflicto**: lanzar un **Chromium propio y aislado** — binario de Playwright (`ms-playwright/chromium-1223`), perfil nuevo en `tmp/e2e/chrome-profile-tester`, puerto CDP propio (9333), manejado por un driver CDP minimo sin dependencias (`tmp/e2e/cdp.mjs`, WebSocket nativo de Node). Es exactamente lo que hace `--isolated`: no toca ningun lock ajeno, y se mata solo al terminar. Con eso las 3 verificaciones que dependen de render CSR (Spotlight, alta de Tienda, Alert de suspension) se midieron **de verdad en navegador**, no por inferencia.
  - **Los 6 checks, uno por uno** (detalle y evidencia en `tasks/e2e-plataforma-retiro-operador.md`):
    - `retiro.mcp.001` ✅ — 404 en 5 variantes (`/api/mcp`, `/api/mcp/http`, `/api/mcp/sse`; apex y subdominio; POST y GET; con y sin Bearer). **Cero 401** ⇒ no quedo handler. El cuerpo es la pagina 404 de Next, no un JSON-RPC de error: no hay route handler que conteste.
    - `retiro.panel.001` ✅ — 404 en los 4 hosts **con sesion valida**. Detalle que importa: sin cookie `/admin/operador` da 307 al login, o sea el 404 SOLO es observable autenticado; medirlo anonimo habria dado un falso verde.
    - `retiro.panel.002` ✅ — rail = 5 anchors exactos; **Spotlight abierto con Ctrl+K real**: `oper`/`operador`/`Operador`/`plataforma` ⇒ los 4 en `nothingFound` con 0 acciones, y controles positivos en la misma sesion (`prod` ⇒ Productos; query vacia ⇒ solo las 5 secciones) que prueban que el buscador estaba funcionando; MenuCuenta abierto por click ⇒ nombre+email+"Cerrar sesion", `[class*=Badge]` **vacio**. Refuerzo estatico: en los 8 chunks JS de `/admin` los literales `Operador de plataforma`/`SinTiendaOperador`/`esOperador`/`/admin/operador` dan **0 hits**; las unicas 2 ocurrencias de "operador" son comentarios del docstring de `env.js` sobre la cuenta R2 (D10, persona operativa).
    - `retiro.panel.003` ✅ — el alta completa renderizada en `www.localhost:3001/admin`. **Requirio un `User` efimero de 0 membresias** (el entorno tiene UN solo `User` y tiene 5 membresias): creado por DB directa, usado, y **borrado en un `finally`** — cleanup confirmado, 0 filas `@e2e.invalid`. Bonus de F04 verificado en el cable, no por grep: `panel.getAccesoActual` devuelve el shape de **2 campos** `{tenants, tiendaActiva}` — el campo `esOperador` ya no viaja.
    - `retiro.editor.001` ✅ — la joya de la corrida: `prueba` resulto ser el caso perfecto de "cuenta logueada SIN membresia" **sin tocar la DB** (existe, PUBLICADA, y el unico User no es miembro). Dueña ⇒ 200 con el editor; sin membresia ⇒ 404; anonima ⇒ 404; host inexistente ⇒ 404. **Los 3 cuerpos de 404 son byte-identicos** (21.932 bytes, mismo sha1) ⇒ I5/D6 no solo "niega", sino que niega de forma *indistinguible*, que es la garantia fuerte que pedia el reviewer de F03.
    - `retiro.suspension.001` ✅ — las 4 puntas, sobre `demo-editorial`. **Estado previo anotado (PUBLICADA) y revertido en un `finally`**; verificacion post-hoc: las 6 tiendas quedan `PUBLICADA` y el storefront volvio a 200. (a) storefront ⇒ 404 **byte-identico** al de un host inexistente, sin filtrar `suspend|operador|soporte`; (b) panel del miembro ⇒ 200; (c) el `Alert` renderiza *"Tu tienda esta suspendida — Mientras este suspendida no aparece publicada y no puede vender. **Escribele al soporte de la plataforma** para reactivarla"* con `href="mailto:nikochaima72@gmail.com"` real, sin boton Publicar, y el panel entero **cero** `/operador/i`; (d) `panel.publicarTienda` ⇒ **HTTP 409 CONFLICT** con el mensaje neutro **y sin cambiar el estado** (el guard corta dentro de la `$tx`, no deja efecto parcial). Captura: `tmp/e2e/suspension-alert.png`.
  - **Design (paso 3.5): 0 findings.** La unica superficie visual que la feature toca es el `Alert` de suspension, verificado renderizado: `rgb(192,62,46)` = token de estado `SUSPENDIDA` (rojo legitimo por `docs/design.md`: estado destructivo/error, no un color suelto), tipografia del theme, cero motion nuevo, cero hex hardcodeado. En la misma captura se ve que las stat-cards del Resumen muestran INGRESOS en tinta y no en verde (regla §5) y el monto como `$0` via `Intl` ⇒ nada que reportar.
  - **Efectos sobre datos del usuario: ninguno neto.** Escrituras hechas y revertidas: `demo-editorial.estado` (PUBLICADA → SUSPENDIDA → PUBLICADA) y un `User`+`Session` efimeros `@e2e.invalid` (creados → borrados). Ninguna sesion del usuario invalidada, el dev server NO se toco, y el navegador de ningun otro agente se cerro. Artefactos de la corrida en `tmp/e2e/` (gitignored).
  - **Estado del plan**: **13 Vitest + el checklist documental de F06 (corrida anterior) + 6 E2E (esta) = todas las Validaciones del plan marcadas.** No toque `status` ni los `state` de las features — esperan decision del usuario.
