# INDEX de tareas

Índice autoritativo de qué está activo. Lo mantienen los agentes del tridente
(`planner` appendea al crear el task file; `feature-tester` mueve a Cerradas al cerrar).
Una fila = una línea corta. El detalle vive en la **Bitácora** de cada `tasks/<slug>.md`.

## Activas

| slug | agente / estado |
|------|-----------------|
| launch-crear-sorteo-y-hardening | implementing — **F01–F04 IMPLEMENTADAS + reviewers verdes 2026-07-24** (`26-07-24-launch-crear-sorteo-y-hardening.md`). Modelo de F01 REFINADO con el usuario (D12–D14): secuencial 1-ACTIVO (guard atómico en $tx), + query `listarSorteosDelTenant`, + sección Historial con ganador, + arrastre de participantes de un sorteo pasado (modal `Radio.Group` → copia RaffleEntry re-ordinaladas en la $tx). F01 crear + F02 editar sorteo desde `admin/sorteo.tsx` (form nativo `datetime-local`, sin `@mantine/dates`; imagen del premio vía `AssetUploader` existente; copy del dashboard corregido). F03 chequeo de monto en el webhook (`montoEsperado` de `Payment.monto` en el núcleo puro, no transiciona si difiere). F04 borrado de `/api/dev/{login,echo-tenant}` + router `post` + registro en `root.ts` (Grep cero refs). Vitest nuevo/actualizado 32/32 verde (filtrado). backend-reviewer APPROVE; frontend-reviewer REQUEST_CHANGES→FIXED (blocker import `Paper` + 3 nits). NO toca schema (cordón con agente paralelo catálogo-v2). **F05 (E2E) y F06 (CSP) DIFERIDOS — no tocados.** **AWAITING USER**: revisión visual de F01–F04 + decisión de drift `frontend-conventions` (3 patrones nuevos). Luego feature-tester. |
| saas-roadmap | implementing — F01 DONE (validada 82/82 + E2E D1 verde, commit 6d5a766). F02 y F05 en planning paralelo (task files propios). Siguientes bloqueos externos: identidad visual (F06), decisiones #3/#4/#5/#6. |
| panel-auth-organizadores | testing — fase F05 del roadmap. **feature-tester 2026-07-17**: `vitest run` COMPLETO verde (28 archivos / 145 tests, 0 fallos); 23 checkboxes Vitest del task file marcados `[x]`. E2E `panel.auth.membresia.001` ✅ (login Google real + membresía en DB); quedan `[ ]` (por instrucción, no por fallo): redirect/productos-CRUD/ventas/config con sesión (no cubiertos en vivo) y `panel.sorteo.ejecutar.001` PARCIAL (sorteo activo + participaciones visto en vivo; ejecución RESERVADA para el usuario — irreversible). **AWAITING USER** para `state`/`status` (+ pendientes previos: shadcn/Mantine, D8 ya cerrado en Tenant.basesSorteo). |
| efectos-post-pago-tenant | testing — F02 del roadmap (`26-07-16-pago-efectos-post-pago-tenant.md`). **feature-tester 2026-07-17**: Vitest 12/12 verde (seed-raffles 3 + aplicarEfectosPostPago 9, dentro de la corrida completa 145/145). E2E backend-only verificado por evidencia DB read-only (order `cmrogl4pi…` PAGADO/`autora`, 1 DownloadGrant + 1 RaffleEntry del mismo tenant, una vez; 1 Raffle ACTIVO por tenant seed) ⇒ ambos checkboxes E2E `[x]`. Schema Raffle/RaffleEntry LANDEADO ⇒ desbloquea el Sorteo de F05. **AWAITING USER** para `state`/`status`. |

| entrega-storage-r2 | testing — F03 del roadmap (`26-07-17-entrega-storage-r2.md`). **feature-implementer 2026-07-17**: F01..F04 IMPLEMENTADAS. `services/storage.ts` (adapter S3/R2, factory config-explícita, presign PUT/GET + headObject) + subida PDF por presigned PUT desde el panel (pdfPath→nullable, schema-guardian APPROVE; `crearUrlSubidaPdf`+`confirmarPdfProducto`; FileInput Mantine) + endpoint público `GET /api/descargas/[token]` (núcleo+wrapper; 302 a URL 10 min; 404 neutral idéntico; defensa I9) + `/dev/descargas` (puente pre-F04). Gates verdes: tsc/lint/**vitest 165/165** (incl. roundtrip real R2, cero regresión). backend+frontend-reviewer APPROVE. **⚠ 2 acciones usuario**: (1) CORS del bucket a mano en Cloudflare (token sin `PutBucketCors` → E2E de subida F02 bloqueado; descarga F03/dev F04 NO dependen); (2) drift de frontend-conventions (subida presigned PUT) + nota CONTEXT.md sugeridos, sin aplicar. Sin commit/feature-tester (instrucción). **AWAITING USER + feature-tester.** |
| correo-transaccional-resend | testing — F04 del roadmap (`26-07-17-correo-transaccional-resend.md`). **feature-implementer 2026-07-17**: F01..F03 IMPLEMENTADAS. `services/correo.ts` (adapter Resend por fetch directo, factory config-explícita, fail-fast, error sin volcar la key) + envío post-pago POST-COMMIT (`conCorreoPostPago` decora el webhook fuera de la $tx, log-and-continue — la venta nunca se compromete) + `domain/correo/` (plantilla pura con disclaimer ADR-0008 + escapado + saneo de cabeceras; use case que deriva todo de la orden server-side, reply-to = membresía más antigua) + reenvío desde el panel (mutation `reenviarCorreoDescarga` + botón Mantine en `admin/ventas.tsx`, regenera grants expirados en $tx). env vars RESEND_API_KEY + APP_URL. **Test real Resend corrido 1 vez** (envío OK con id a `nikochaima72@gmail.com` — el `+test` lo bloquea la regla sandbox de Resend con remitente sin dominio verificado). Gates: backend+frontend+change-set reviewer APPROVE; **vitest 190/190** (+1 skip opt-in), lint exit 0, tsc limpio salvo `tmp/e2e-descarga.ts` (scratch AJENO gitignored de F03 — decisión del usuario: borrar/excluir tmp/tsconfig/dejar). Sin commit/push/feature-tester (instrucción). **2 drafts de drift de docs pendientes de permiso** (backend-conventions: correo post-commit; frontend-conventions: mutation por-fila). **AWAITING USER (tmp + drift) + feature-tester.** |
| storefront-plantilla | testing — F06 del roadmap (`26-07-17-storefront-plantilla.md`). **feature-implementer 2026-07-17**: F01..F06 IMPLEMENTADAS. Schema aditivo (heroTitulo/heroSubtitulo/avisoTexto, schema-guardian APPROVE + db push). Storefront mobile-first en el subdominio: theming per-tenant (`overrideDesdeBranding` hex→escala Mantine, helper puro sin dep; `mergeThemeOverrides` en `_app`), resolver de branding SSR, catálogo+detalle (getProductoStorefront tenant-scoped), carrito client-side per-slug (localStorage, sin cuenta ADR-0004), checkout→Flow con URL de retorno derivada del subdominio (D6, webhook global intacto), sorteo público (getSorteoActivoStorefront, solo conteo, sin correos) + disclaimer obligatorio (ADR-0008), apex placeholder neutral. Limpieza: dev/checkout* + maquetas landing eliminadas, embla desinstalado. Gates: tsc+lint verdes, **vitest 215/216 (+1 skip)**, backend+frontend-reviewer APPROVE. Verificación visual en :3001 OK (autora #e11d48 / prueba #0d9488, aislamiento, apex neutral, 404s). **Pendientes**: (1) drift `frontend-conventions` (fecha() + 3 patrones nuevos) sin aplicar; (2) branding demo puesto en tenants seed (revisable). Sin commit/feature-tester (instrucción). **AWAITING USER + feature-tester.** |
| sorteo-por-producto | testing — feature aditiva (`26-07-17-sorteo-por-producto.md`, ADR-0012). **feature-implementer 2026-07-17**: F01..F04 IMPLEMENTADAS. Schema aditivo (`Product.participaEnSorteo`, `OrderItem.cantidad`+`participaEnSorteo` snapshot, `RaffleEntry.ordinal` + `@@unique([raffleId,orderId,ordinal])`; schema-guardian APPROVE; db push con `--accept-data-loss` AUTORIZADO por el usuario tras verificar 0 duplicados). Checkout por cantidad (input `items:{productId,cantidad 1..99}`, total = Σ precio×cantidad en Decimal server-side, snapshots congelados); stepper +/− reusable en carrito/catálogo/detalle/checkout. Efectos post-pago por ticket (K = Σ cantidad de participantes ⇒ K RaffleEntry ordinal 0..K-1, idempotente; DownloadGrant sin cambio). Panel: Switch "Participa en el sorteo" + badge Sorteo; conteo por ticket (`totalParticipaciones` + participantes agrupados por correo con tickets); `ejecutarSorteo` sin cambio de algoritmo. Storefront relabel "participaciones". Seed autora=true/prueba=false + dev DB sincronizada. Gate VERDE: tsc+lint+**vitest 264/265 (+1 skip)**. Reviewers TODOS APPROVE (schema-guardian/backend/frontend/change-set, 0 blockers). Sin commit/push/feature-tester (instrucción). **AWAITING feature-tester** (marcar `[x]` + 4 E2E asistidos: cantidad/tickets/toggle/panel) **+ usuario**: 2 drafts de drift (frontend-conventions StepperCantidad, prisma-conventions lección del swap de unique) sin aplicar. |
| plantilla-rica | testing — extiende F06 (`26-07-17-plantilla-rica.md`). **feature-implementer 2026-07-17**: F01..F04 IMPLEMENTADAS. **Assets públicos de marca en bucket R2 PÚBLICO** separado del privado de PDFs (ADR-0013): `services/storagePublico.ts` (presigned PUT + headObject + composer URL pública con cache-buster, reusa el S3 client de storage.ts) + env `R2_PUBLIC_BUCKET`/`R2_PUBLIC_BASE_URL`. Schema aditivo (schema-guardian APPROVE + db push): Tenant hero/redes/contacto + Raffle.premioImageUrl. Subida en el panel (logo/hero/portada/premio, destino discriminado, key per-recurso server-side; logo+portada pasan de texto a asset subido — D4/I6, cerrado por backend-reviewer). Storefront RICO: 7 secciones design.md §5.1 (header+countdown, hero 2-col+trust badges, catálogo con portadas+badge Sorteo, vitrina del premio+disclaimer ADR-0008, cómo funciona, footer redes/atribución neutral), mobile-first, tematizado per-tenant (gradientes derivados de la escala, cero hex inline) con degradación elegante COMPLETA §5.2. Reviewers TODOS APPROVE (schema-guardian/backend/frontend/change-set; 1 blocker de lint + `portadaUrl` free-text corregidos). Gate VERDE: tsc + lint 0 + **vitest 290/2-skip (54 archivos)**. Verificación SSR :3001 (autora rosa / prueba teal sin redes / apex neutral). Seeds: branding demo idempotente. Sin commit/feature-tester (instrucción). **Pendientes usuario**: (1) crear el bucket R2 público real en Cloudflare (env vacías) para el E2E de subida real de imágenes; (2) 2 drafts de drift de frontend-conventions sin aplicar. **AWAITING feature-tester** (Vitest ya verde; E2E asistido) **+ usuario.** |
| identidad-talonario | implementing — **F01–F05 IMPLEMENTADAS + reviewers verdes 2026-07-18** (`26-07-17-marca-identidad-talonario.md`). F01: theme «El Talonario» (cobalto/amarillo/tinta, `primaryShade: 6` alinea tenantTheme, Bricolage/Instrument/Plex, screens Tailwind↔Mantine, `crema`→`hundido`, favicon/og, design.md reescrito, frontend-conventions drift aplicado, nota SUPERSEDED en admin-marca); tests 24/24. F02: gramática talonario en `src/components/landing/` (CSS module acotado cero-hex + Wordmark «éa»). F03: landing oficial en el apex (bandas D9, FAQ con Mantine `Accordion`, `PlaceholderPlataforma` reemplazado; despacho por zona/tenant INTACTO I2). F04: login split cobalto. F05: `motion@12.42.2` scroll-reveal acotado a landing (reduced-motion respetado). Reviewers frontend APPROVE en todas. **F06 (borrado de `/prototipo/*`) PENDIENTE de confirmación del usuario** — no ejecutado. Luego `feature-tester` (E2E browser-verify de F01–F05). |
| ui-migracion-mantine | testing — migración shadcn→Mantine 7 IMPLEMENTADA (ADR-0011). **feature-implementer 2026-07-17**: F01..F04 completas — Mantine 7.17.8 (core/hooks/form/modals/notifications) cableado, login + 5 páginas admin + admin-layout(AppShell)/estado-badge/stat-card migrados 1:1, shadcn retirado (ui/ + components.json + 9 deps huérfanas fuera). Gates verdes: tsc/next lint exit 0, vitest **145/145** (sin regresión server). frontend-reviewer APPROVE (0 blockers, 1 nit corregido). Sin commit/feature-tester (instrucción). **AWAITING USER**: E2E visual con sesión (sesión principal), + 2 decisiones abiertas (productos desactivar/eliminar-confirm; convención `ta` vs `text-right` en tablas). |

| page-builder | testing — carril A COMPLETO (`26-07-17-page-builder.md`). **feature-implementer 2026-07-18 (nocturno)**: F01–F12 en 4 fases, 12/12 reviewers APPROVE. StorefrontPage jsonb draft/published + versiones append-only con rollback; registro Zod 13 widgets; render público solo desde published (backfill 1:1) + preview tokenizada; MCP /api/mcp 12 tools (Bearer); CSP Report-Only + EmbedFrame sandbox; sesión wildcard + banner dueña; bug real corregido (matcher middleware sin '/'). Gate árbol combinado 406/407 + tsc/lint 0. Commit fddcc4c pusheado (deploy auto). **AWAITING feature-tester** (E2E browser: equivalencia visual F05, round-trip MCP, banner) **+ usuario**: addendums ADR-0016/0019, 5 drifts de conventions, env prod MCP_OPERADOR_TOKEN/preview. |
| builder-tanda-1 | testing — tanda 1 de fidelidad COMPLETA (`26-07-24-builder-tanda-1.md`). **2026-07-24 (nocturno)**: F01–F14 implementadas y validadas — colorAcento (2a escala), bicolor, hero acentuado+eyebrow+destacado, ticker sobre nav, nav auto-derivado, alturas svh, mascaras, confetti, patch en vivo, auto-save, dnd. Reviewers: schema+backend+frontend APPROVE. Tester visual final: replica landing_idol en prueba ~90% (Bebas viva, 8 fixes F13 en vivo, hero compactado) + tienda NUEVA bcac replica de tienda-libro ~80% (holocard animada+tilt ajustada a 4:3). 6 gaps de fidelidad anotados para tanda 2 (vitrina_proximamente, pack tickets, ancho card, holo en hero split, eyebrow independiente, stage-lights). Suite 578/580 (1 rojo preexistente de WIP marca). Commits 4a6bd1f..71eba96+ pusheados. **AWAITING USER** para state/status + pendiente: confetti E2E con Flow sandbox. |
| catalogo-v2-builder-visual | testing — v2 del builder (`26-07-18-catalogo-v2-builder-visual.md`). **2026-07-19**: F01–F10 COMPLETAS con reviewers verdes + feature-tester en 2 pasadas: Vitest repo 483/484, E2E navegador del EDITOR VERDE (agregar/props/estilo/reorden/guardar/publicar/historial/rollback en Chrome real), 2 bugs encontrados y CORREGIDOS (count-up async, TemaPagina oscuro via root.props descartado en migrate) + CORS de subdominios arreglado en ambos buckets. Commits b56a3ad+db0fd77+fa9ee13 pusheados. Pendientes E2E menores: reduced-motion en vivo, hero variantes, round-trip MCP visual, subida de imagen (ya desbloqueada por CORS). **AWAITING USER** para state/status + REVISABLES (labels humanos F10.5, drift conventions). |
| admin-dashboard-resumen-rediseno | testing — rediseño COMPLETO del panel (`26-07-18-admin-dashboard-resumen-rediseno.md`). F01–F03 hechas y **verificadas en vivo por el usuario** (chrome «Oscuro + calmo» + Resumen). **feature-implementer 2026-07-19 (run F04–F08)**: las 5 páginas restantes migradas a la gramática nueva, una feature por página, **5/5 frontend-reviewer APPROVE (0 blockers)**. F04 Ventas + F06 Productos → `PanelCard padding={0}` edge-to-edge (tablas full-page sin header). F05 Sorteo → PanelCard + caja ganador inset borderless. F07 Configuración → SettingCard→PanelCard + estado Flow inset borderless (write-only intacto). F08 Operador → PanelCard (gate FORBIDDEN intacto). Cero `<Card>` residual. Lógica/queries/mutations/guards intactos (I6). NO suite completa (rol tester), NO commit, cordón con catálogo-v2 respetado. **DRIFT pendiente a proponer** (2 sub-patrones para `frontend-conventions.md`: 2 variantes de tabla del panel + inset borderless anidado). **AWAITING USER** (revisión visual F04–F08 + decisión drift) **+ feature-tester** (Vitest F03 ya verde; E2E de las 5 páginas). |
| admin-marca | testing — carril B COMPLETO (`26-07-17-admin-marca.md`). **feature-implementer 2026-07-18 (nocturno)**: F01–F07, 4 reviewers verdes. Identidad "En Vivo" (fusión ruta C × investigación: violeta #7239d5, crema cálida, Manrope+Sora, semántica teal/ámbar/ladrillo en theme — cero hex inline), chrome invertido (wordmark+chip+avatar+Ver mi tienda), PageHeader, login con marca, EmptyState ×4, favicon/OG, Spotlight Cmd+K + dark toggle. Commit 153b9ef pusheado. **AWAITING feature-tester** (E2E visual con OAuth) **+ usuario**: fusión de valores, drift conventions aplicado directo, OG raster pendiente. |

| builder-tanda-2-fidelidad | testing — gaps de fidelidad cerrados (`26-07-25-builder-tanda-2-fidelidad.md`). **2026-07-25**: F01–F10 (vitrina_proximamente, hero split con visual/holocard, ancho card, eyebrowEstilo, stage-lights, padTop/padBottom, patrones); F11 (3 tiendas demo K-pop); F12 (fidelidad demo-dreamy: contraste, packs_precio, momento_ticket, hero glassy, stats tarjetas_suaves); **F13 (corrección visual del catálogo: covers de LIBRO tematizados —set curado de gradientes de familia, adiós hue-rotate/azules—, `catalogo.layout` carrusel, badge `Nuevo` derivado, hero visual `motivo` tickets/estrella, stats compactas; re-seed demo-dreamy rev4)**; **F14 (fidelidad >95% de las 2 réplicas restantes: 2 aditivos curados — esquema `marfil` off-white cálido dark-aware para editorial [EL gap del 78%] + ambiente `neon` glow concentrado saturado para concert; resto por DATOS: noche carrusel/packs_precio/momento_ticket/tarjetas_suaves/holo+tickets/topbar limpio; editorial marfil shell/método papel cálido/premio caja bordeada/sin strip; re-seed demo-noche+demo-editorial rev5)**. Re-replicas publicadas (bcac rev4, prueba rev5, demo-dreamy/noche/editorial rev5). Regresión styles+pagebuilder+mcp+editor+checkout+scripts 295/295 (F14); curl VERDE (marfil/neon en shells, packs/momento/holo/Playfair/Anton, ticker AUSENTE) + autora/prueba/bcac/demo-dreamy INTACTAS (marfil/neon-leak 0). Pack de tickets = decisión de producto pendiente del usuario. **AWAITING frontend-reviewer (F13 `catalogo`/`storefront-hero`/… + F14 `estiloSeccion` marfil/neon) + backend-reviewer (F14 `tools.ts` vocabulario MCP), orquestador + feature-tester** (screenshots lado a lado editorial/noche vs `dev-ref/` + % fidelidad). |

| builder-tanda-3-arquitectura | implementing — **F01–F07 IMPLEMENTADAS (corrida nocturna 2026-07-26, Fases 0-3 del prompt)** (`26-07-25-builder-tanda-3-arquitectura.md`). F01 motor de runs (RichTexto/marcas enum/links tipados/RunsTexto/texto_rico v2 migrate LOSSLESS); F02 runs en hero (v3 absorbe tituloAcento, +marca/gradiente marks) + perfil_autora.bio (v2); F03 EditorRuns contenteditable plaintext-only (helpers puros `runs-edicion.ts` + control `runs` en form-props); F04 multi-página backend (`[slug].tsx` SSR fail-closed, use cases páginas $tx, `StorefrontPage.enNav` aditivo, nav derivado + enNav defensivo); F05 panel Páginas (DockKey, switcher `?pagina=`, home protegida); F06 chrome editable (`Tenant.chromeJson`, ChromeSchema, pinned por construcción I-U2, menú manda sobre nav, ADR-0021 **propuesto**); F07 setChrome + panel Chrome (pinned con candado). Gate: tsc 0 nuevos (2 ajenos de pago in-flight), lint 0, vitest área **387/387** + checkout 41/41. Curl :3001: 6 tenants byte-idénticos (migrate no-op), routing OK, /editor compila. **F08–F15 (fila/responsive/UX/chat IA/cierre) = OTRA corrida.** AWAITING orquestador: schema-guardian (enNav+chromeJson), frontend/backend/change-set reviewers, **restart dev server** (cliente Prisma nuevo, generate EPERM por lock), aceptar ADR-0021. Sin commit/push. D1–D22 REVISABLES anotadas en Bitácora por fase. |

| videos-hyperframes-fabrica | testing — **F01–F04 IMPLEMENTADAS** (`26-07-24-videos-hyperframes-fabrica.md`). Fábrica HyperFrames portada a `videos/hyperframes/`: F01 131 tokens `--st-*` derivados de verdad desde `theme.ts` con `npx tsx` (+ `tokens.local.json` sin un solo hex, gate de drift, `--check`); F02 pipeline completo en Windows (scripts + `_lib/tour-kit` con el panel «Oscuro + calmo» + gramática del talonario; `assets/` per-video EFÍMERO vía `materializar.mjs`; 3 gotchas nuevos 13-15 documentados, incl. el asterisco-barra que rompe el `.js` en silencio); F03 harness (`hyperframes-video-builder` + skill `capsula-video`); **F04 piloto «Configura la tienda» RENDERIZADO** — spec cerrado → subagente (1 sola iteración) → `out/2026-07-25-configura-la-tienda.mp4` 1920×1080@30fps, 39,5 s, **mudo** (ffprobe 0 streams de audio), 4 frames del MP4 real leídos por el implementer (I5). frontend-reviewer APPROVE ×2 (0 blockers; 3 nits de fidelidad corregidos **y re-renderizados** para no dejar el MP4 fuera de sync). Mock validado contra `configuracion.tsx` (el `alignItems:"start"` del SimpleGrid real confirma las alturas distintas de las cards). Sin Vitest/E2E por D8; sin commit. **AWAITING feature-tester** (gate de fábrica: check-classes + lint + render + frames) **+ usuario**: párrafo de `CLAUDE.md` drafteado sin aplicar (fuera del rol del implementer) y 2 candidatos a promover al kit (`.pnl-subhead`, `.pnl-toast`) diferidos a la 2ª cápsula. |



| sistema-correos-comprador | **testing — PLAN COMPLETO: F05 (consentimiento/supresión/baja) y F06 (recordatorios T-48h/T-6h) IMPLEMENTADAS 2026-07-28, reviewers verdes. No queda feature sin escribir.** **F05**: schema nuevo vía `schema-guardian` (APPROVE-WITH-CHANGES — su cambio caro fue de NAMING y no cosmético: `emailNormalizado` para la identidad + `email` para el snapshot, porque en `CorreoEnviado` la columna `email` significa lo CONTRARIO y cruzarlas es un miss silencioso por casing) — `ConsentimientoRecordatorios` + `SupresionCorreo` (DOS tablas y no una: fundirlas le daría al endpoint público sin login un camino de escritura hacia la PRUEBA legal del consentimiento) + `Tenant.identidadLegal`, push aditivo. Checkbox NO premarcado sostenido en TRES capas (form, schema sin default, y `=== true` en el use case, que es la que cubre callers que no pasan por Zod); el texto lo pone el SERVER desde una constante compartida —dejar que el navegador mande la prueba sería que el evaluado escriba su descargo—; IP desde `ctx.ip` nuevo. **El consentimiento NO se escribe con `upsert`**: corre dentro de la `$tx` del checkout y en Postgres un statement que falla aborta la transacción entera, así que un P2002 se habría llevado puesta LA VENTA ⇒ `createMany({skipDuplicates})` + `updateMany`, que no lanzan nunca. Endpoint público de baja idempotente por constraint, con **GET que pregunta y POST que ejecuta** (los escáneres corporativos siguen los enlaces de un correo: un GET que suprimiera daría de baja a quien nunca hizo click — es la razón de RFC 8058) y respuesta neutral para token desconocido. Remitente promovido a config con el tipo `ClaseDeCorreo`, que gobierna a la vez el buzón (seam D2) y las cabeceras RFC 8058 (I5), + un guard que falla si una plantilla vuelve a escribir el dominio como literal. **F06**: la resolución del choque del plan entre «ventana [48,49)» y «corrida perdida se recupera» — cada offset es un TRAMO abierto hacia abajo cuyo piso es el techo del siguiente (`(7h,49h]` y `(0,7h]`), contiguos y sin solape (prueba de mutación: bajar el piso a 6 h pone 2 tests en rojo). Eso impone que el copy **no pueda decir «faltan 48 horas»** (en recuperación mentiría) y hay un test que lo prohíbe. Techo de 3 por construcción (N offsets + clave con offset + unique), no por contador. El filtro de I5 se re-pregunta AL ENVIAR y no solo al encolar (bajo cuota Free una fila espera días: quien se dio de baja el martes no recibe lo encolado el lunes). Retiro de obsoletos a `FALLIDO` porque una fila PENDIENTE irresoluble tapa la ventana del drenado para siempre — el head-of-line de F04 un piso más abajo. El cron gana un paso productor antes del drenado, bajo el mismo gate. **1 BLOCKER del `backend-reviewer`, corregido**: una Tienda `EN_PAUSA_POR_PAGO` **sigue `PUBLICADA`** (ejes separados, ADR-0026), así que el T-6h le habría mandado «todavía alcanzas a comprar» con enlace a un storefront que sirve la página neutral ⇒ los tres caminos ahora delegan en `evaluarGateVenta`, la MISMA función pura del checkout. `frontend-reviewer` APPROVE de una (1 nit aplicado: los hex de la página de baja anotados con su token). Vitest FILTRADO **127 verdes** (106 DB-free + 21 DB-backed); `tsc` y `eslint` limpios; gate visual rasterizado y MIRADO (`tmp/preview-correo-recordatorio-{48h,6h}.png`). **Higiene de prod**: la DB de dev ES la de producción ⇒ censo final del ledger GLOBAL **2 filas, 0 PENDIENTE** (ambas de otro carril), 0 consentimientos, 0 supresiones, 0 tenants de fixture; **cero correos reales enviados, cero cuota consumida**. **PENDIENTES anotados y no ejecutados**: drift de `design.md` §9 (la excepción de hex nombra solo a `layoutCorreo.ts`), el head-of-line ENTRE tipos resolubles como riesgo de ADR-0027 §5, y 2 nits de cobertura de concurrencia. **REVISABLES del usuario**: el texto del checkbox (es la prueba legal que se persiste), que el checkbox solo aparezca con Sorteo ACTIVO, y que hoy la baja sea definitiva por Tienda (no hay re-suscripción). Estado previo — F04 (C4/C5 resultado del sorteo) IMPLEMENTADA 2026-07-27, reviewers verdes; suma a F03+F08 que ya esperaban al `feature-tester`. **F04**: ejecutar el sorteo encola en el ledger, dentro de su misma `$transaction` y solo en la rama que ganó el guard atómico, 1 fila `RESULTADO_GANADOR` + N `RESULTADO_NO_GANADOR` (dedup por IDENTIDAD —el email normalizado de la clave, no el string— para que nadie reciba «ganaste» y «no ganaste» a la vez); dos plantillas nuevas sobre el layout del tenant (el ganador con premio + su boleto + canal de contacto; el de no ganador con el número ganador y los suyos, **sin identidad del ganador ni promo**, D4 garantizado por TIPOS: `SorteoResuelto` no tiene dónde meter ese dato); derivación batch de 3 queries con fail-closed triple (sorteo inexistente / tenant que no calza / sorteo sin ejecutar). **Fix del head-of-line blocking que F03 dejó anotado**: el seam pasó a `ResolvedorDeCorreos = {tipos, armar}` y el drenado filtra por tipo ANTES del `take` — sin eso, ≥N filas viejas de un tipo sin plantilla bloqueaban las confirmaciones para siempre. El registro de resolvedores se mudó del wrapper Next a `domain/` (era intesteable; el tester de F03 había tenido que escribir un espejo). **1 vulnerabilidad real cazada por el TDD**: el asunto nuevo lleva el nombre del sorteo, o sea texto del Organizador en una cabecera — un `\r\n` inyectaba una cabecera. **1 BLOCKER del `frontend-reviewer`**: la notification prometía que los correos «salen dentro de la hora», falso con >100 destinatarios bajo Resend Free (el propio código cita 300 participantes como escala normal) ⇒ «empiezan a salir» + matiz completo en el modal. **T6 centralizado** (`Tenant.contactoEmail ?? membresía más antigua`) en un helper que ahora usan C1 **y** C4/C5 — es el único cambio de comportamiento fuera del alcance literal de F04 y toca una feature `passing`, anotado para que el usuario pueda darlo vuelta. **Pendiente heredado RESUELTO sin AWAITING**: la `$tx` de ejecutar sorteo es **plana en N** (medido: 2439 ms con 3 participantes, 2456 ms con 300 — el `createMany` es un round-trip), o sea F04 no reactiva la pregunta del timeout de producción. Vitest filtrado del área verde (plantillas 7, derivación+resolvedor 6, ledger 6, drenado 10, ejecutarSorteo 12, C1 sin regresión 10+12); `tsc` limpio en todo el repo; 2 mutation tests (dedup ingenua y filtro de tipos) confirman que los tests no son vacuos. **AWAITING USER**: (a) permiso para el drift de `backend-conventions.md` (§ Jobs: la regla del filtro por tipos + la referencia al registro de resolvedores); (b) confirmar/sacar los 2 checkboxes que agregué a Validaciones (cobertura extra + verificación contra prod que pidió el `backend-reviewer`); (c) el copy nuevo del modal/notification del panel es REVISABLE. Estado previo — **F03 + F08 listas para el `feature-tester` 2026-07-27 (relevo #4)**. `26-07-26-correo-sistema-correos-comprador.md`, ADR-0024 + ADR-0027. **F03 (C1 confirmación de compra) COMPLETA**: la plantilla de descarga creció a confirmación —boletos D10 con prefijo D12, sorteo NOMBRADO (I10), cierre en hora de Chile (I7), resumen de orden en CLP, bases— con la fila del ledger encolada dentro de la `$tx` post-pago (antes de los `return`, así que una orden sin tickets igual recibe su correo), envío post-commit por `waitUntil` de `@vercel/functions` que reclama la fila antes de mandar (I2/I3), resolvedor enchufado en el cron con guard de tenancy, reenvío del panel migrado, y la **landing Q1-e cerrada** (`landing.faq.005` reescrito + `landing.faq.007` nuevo). **1 BLOCKER REAL cazado en la pasada de verificación que los APPROVE previos no habían atrapado**: la `Idempotency-Key` del envío automático se colaba en el reenvío manual ⇒ Resend devolvía la respuesta cacheada 24 h, el panel decía «reenviado» y al Comprador no le llegaba nada (fix: `ModoIdempotencia` obligatorio; `reenvio.007` lo fija). **2 tests quedaron en carrera** desde que F03 volvió el decorator fire-and-forget — corregidos con el recolector del seam `ProgramarTarea`. Reviewers: backend APPROVE tras el fix, frontend APPROVE. Vitest filtrado **159/159 (+1 skip)** en 13 archivos, `tsc` sin errores propios. **F04–F06 `not_started`, intactas.** **AWAITING USER**: (1) el **drift de `frontend-conventions.md` de F08** (gotcha `className`/`Input.Wrapper`) sigue SIN aplicar, esperando permiso; (2) ¿subir el `timeout` de las `$transaction` de PRODUCCIÓN? (`confirmarPagoDeOrden`/`reenviar` corren con el default de 5 s y desde esta máquina flakean — medido ~1.000 ms/statement; en Vercel sobra, y subirlo alarga el row-lock del `Raffle`); (3) 2 design findings del correo (enlace de bases pegado, `Total` sin jerarquía) registrados para la tanda de pulido ya diferida. Nota para F04: el **head-of-line blocking** del drenado sigue sin dueño y F04/F06 lo activan. Estado previo — **2026-07-26 (3ª vuelta del planner)**: F01 **done** (Número correlativo; Vitest 50/50 + E2E 1/1). F02 **passing** (máquina de envíos: ledger `CorreoEnviado` claim→send→confirm, cron `/api/cron/correos` + `CRON_SECRET`, adapter batch, cuota Resend Free tolerada). F07 **passing** — rework D9-rev (marca del TENANT: logo + `colorPrimario`, tickets D10) validado 93/93 + 2 envíos reales `delivered` + **visto del usuario en Gmail**; pulido fino de estilos DIFERIDO a después del sistema (fuera del plan). **F08 NUEVA (D12): prefijo de ticket configurable** (`Tenant.prefijoTicket`, panel Configuración, aplicado solo en presentación vía `numerosDelSorteo.ts` — panel y correos muestran `ARMY-1043` idéntico), secuenciada ANTES de F03. F03–F06 `not_started`. **Orden para el implementer: F08 → F03 → F04 → F05 → F06** — puede arrancar ya, sin AWAITING. `CRON_SECRET` y `maxDuration: 300` ya resueltos en local y Vercel |
| productos-tipos-digitales | **testing — ENMIENDA v2 COMPLETA (F10–F13 implementadas, reviewers verdes, 2026-07-26). Lista para el `feature-tester` (F14 = cierre E2E).** Un pack ES un producto: `Product.fuenteId` + `unidadesPorPack`; fuente ESTANDAR ⇒ N copias derivadas en presentación (0 filas), fuente SOBRE ⇒ asignación aleatoria del pool de la FUENTE; la colección nunca se vende directo ni sale en el catálogo; checkout de vuelta a `{productId, cantidad}`; `/producto/[id]` retirada (307); `packs_precio` fuera del menú con deprecación suave. **backend-reviewer APPROVE + frontend-reviewer APPROVE, cero blockers** (1 pasada única sobre F11+F12+F13, autorizada por el usuario). 2 NITs aplicados: la regla «de dónde salen los archivos» estaba escrita 3 veces ⇒ unificada en `origenDeArchivos`/`entregaAlAzar` (`server/productos/fuenteDeArchivos.ts`), y `aria-label` contextual en los botones del catálogo. `tsc` 0 errores; Vitest filtrado **44/44**. **Pendiente del tester**: los 2 checklists E2E siguen describiendo el flujo muerto de `ProductPackOption` y hay que reescribirlos; los flows (b)/(c) necesitan el túnel de Flow vivo y re-montar los insumos de `prueba` al modelo v2. **2 flakes de entorno, NO de la feature**: `efectos.numero.002` cae por el timeout de 5 s de `$transaction` contra Supabase remoto (decisión abierta del usuario, ver fila de correos), y un FK `DownloadGrant_orderId_fkey` que NO se reprodujo en aislamiento (contención de DB compartida con otro carril). Estado previo: **ENMIENDA v2 escrita 2026-07-26, aprobada por el usuario** (`26-07-26-productos-tipos-digitales.md`, sección «ENMIENDA v2»). **La v1 (E6–E12: modal, «Cómo se vende», switch, tope 3, c/u) fue RECHAZADA por el usuario por exceso de ceremonia** ("un pack, n productos, tiene este precio y listo"). Modelo v2: **un pack ES un producto más** — 2 campos extra (`Product.fuenteProductoId` self-relation + `Product.unidades`); fuente ESTANDAR ⇒ N copias derivadas, fuente SOBRE ⇒ asignación aleatoria del pool de la fuente (backend F08 intacto); SOBRE nunca se vende directo ni sale en catálogo; sin widget nuevo (E18: «Nuestros packs» = catálogo modo selección); `/producto/[id]` muere igual (E2). F10–F14: schema aditivo, panel con selector de fuente (muere `opciones-de-pack.tsx` y con él el bug F06 del form anidado — sin fix, la UI se borra), checkout vuelve a `{productId, cantidad}`, storefront, cierre E2E (flows b/c con túnel; orden PENDIENTE `cms2apsqx000cnik84txlnr0y` solo fixture; tRPC 404 del apex en `/entrega`). `ProductPackOption` deprecada SIN drop. Cross-carril re-verificado en `storefront-tema-paginas-plataforma` (sigue perdiendo `/producto/[id]`). Estado previo: Vitest 229/229 verde, E2E parcial (2ª vuelta). `26-07-26-productos-tipos-digitales.md`. **F01–F09 implementadas** (fase 1 tipos + fase 2 sobre sorpresa). Corrida del tester: DB-free 176 + DB-real 53 (corridos con `--no-file-parallelism` para eliminar auto-contención) — **la contención de DB que anunció el implementer NO se reprodujo**, cero reintentos, cero rojos; los 2 tests de integración real contra R2 corrieron de verdad. **E2E bloqueado por infraestructura, no por la feature**: los DOS carriles de navegador MCP estaban tomados (otra sesión con 12 procesos chrome activos) y `browser-verify` §0 prohíbe expropiarlos, + el túnel cloudflared de `FLOW_URL_CONFIRMATION` está CAÍDO (curl 000) ⇒ sin webhook no hay `PAGADO`. **Verificado igual en vivo por curl, sin escribir en DB**: `/entrega/<token>` responde **200 en el APEX** (cierra la regresión que el backend-reviewer cazó en F09) y da cuerpo **byte-idéntico** en apex y subdominio; el corte `?archivo=` da 302 para lo autorizado y **404 neutral** para archivo de otra Tienda / id inexistente / token basura; la entrega redirige a R2 con `X-Amz-Expires=600` + `response-content-type` + `filename*` RFC 5987 del `ProductFile`; **I2 con 0 fugas de key**; la FAQ de la landing ya nombra las 5 familias + 20 MB (también en el JSON-LD). **HUECO A DECIR EN VOZ ALTA: la fase 2 nunca se ejerció contra datos reales** — censo de DB al cierre: `SOBRE=0`, `ProductPackOption=0`, `PackAssignment=0`, `ProductFile` PDF=4 y ningún tipo nuevo. **DECISIÓN DEL USUARIO (cierra el AWAITING ANSWER de F08)**: pool encogido entre compra y pago = **opción A**, queda el default implementado (entregar lo que alcanza, la orden pagada JAMÁS se revierte, grant igual, `console.error` sin PII). **BACKLOG derivado**: la **opción C — visibilidad del incidente en el panel del Organizador** (marcar la orden/línea con entrega parcial y listarla, en vez de que el rastro dependa de que alguien mire los logs) queda como **carril futuro**; al aterrizar necesita schema nuevo (columna o tabla de anomalías) + superficie de panel. **AWAITING USER**: estado de las features + si libera un carril de navegador para la tanda E2E que falta. Detalle original del implementer abajo. F01–F08 implementadas: `ProductFile` unificado (el archivo del producto pasa de la columna `pdfPath` a una tabla, con tipo/MIME/tamaño/key) + `Product.modalidad`; subida y entrega generalizadas a la allowlist completa (PDF/EPUB/PNG/JPEG/WebP/MP3/M4A/WAV/ZIP), con el tope de 20 MB verificado server-side por `headObject` y el tipo DERIVADO del MIME validado, jamás del nombre del archivo; gate de publicación a «≥1 producto activo entregable»; **sobre sorpresa** (pool de archivos + opciones de pack con precio `Decimal`, snapshot `{unidades, precio}` en `OrderItem`, y asignación aleatoria post-pago sin duplicados dentro de la misma `$transaction` que Entitlement/tickets, idempotente ante replay del webhook). En manos del `feature-tester` |
| storefront-focos-ambiente-animados | planning — **PLAN ESCRITO 2026-07-27, AWAITING USER APPROVAL** (`26-07-26-storefront-focos-ambiente-animados.md`): luces de ambiente animadas por CSS puro (sin JS), 4 features — F01 shell `TemaPagina.ambienteAnimado` (default false = byte-idéntico), F02 hero `imagen_fondo` props `luces`+`lucesAnimadas` (capa entre overlay y texto — el caso iselk), F03 controles del editor, F04 activación curada iselk+demo-noche. 7 tests Vitest aprobados en grill; inspector/visual a E2E. Reduced-motion ⇒ estático; transform/opacity GPU; cero hex |
| storefront-carrito-total-y-drawer | testing — **F01–F05 IMPLEMENTADAS + 4 pasadas de reviewer verdes 2026-07-27** (`26-07-27-storefront-carrito-total-y-drawer.md`, task chica especificada directo por el usuario con screenshots, sin planner previo). **F01** total del carrito SERVER-SIDE: query `checkout.cotizarCarrito` (`tenantProcedure`, `Decimal`, precios VIGENTES releídos de la DB) + hook `useCotizacionCarrito` compartido — **I4 intacto: el cliente solo PINTA**, y la cotización reusa la MISMA regla de vendibilidad que `iniciarCheckout` para que el total mostrado no incluya nada que el checkout vaya a rechazar; el array de ítems se extrajo a un schema Zod compartido. **F02** drawer en flex-columna (lista scrolleable + footer fijo). **F03** miniatura en drawer y checkout (`portadaUrl?` en el carrito con rehidratación tolerante + `rehidratarCarrito` extraída PURA; la del server gana). **F04** «por pack de N» solo con N ≥ 2, con la regla en `leyendaPrecioUnitario` — una definición para catálogo, drawer y checkout. **F05** portada del producto en `/entrega/[token]` para lo que no tiene miniatura presignada. Reviewers: backend APPROVE ×2 (sin fugas cross-tenant ni de buckets, `Decimal` puro), frontend APPROVE ×2; **0 blockers, 12 nits de los que apliqué 9** — el más caro fue el `overflow-y:auto` que Mantine le pone al `content` del Drawer (habría devuelto el bug del footer en iOS) y el rediseño de la fila a 2 líneas, que salvó el título a 320 px. Vitest **filtrado** verde (checkout 61/61, components+storefront 218/218, entrega 4/4 contra DB real); tsc + lint limpios; verificado en vivo por curl en :3001 (total real, aislamiento cross-tenant, copy viejo ausente). Suite completa y TODO lo visual = del `feature-tester`. Sin commits. **AWAITING USER**: permiso para 3 drifts de doc + 3 nits diferidos (`.max()` del array de ítems, chip de tipo en la entrega, bug PREEXISTENTE del `<img>` sin `onError` en el catálogo) |

## En pausa

| slug | razón |
|------|-------|

## Cerradas recientes

| slug | cierre |
|------|--------|
| entrega-postpago-retorno-y-reacceso | done 2026-08-18 — entrega al estándar de venta digital manteniendo sin-cuentas (research de mercado en .scratch/entrega-postpago/): F01 grants SIN vencimiento (expiresAt nullable = seam de revocación; backfill 14/14 con 2 compradores reales recuperando acceso), F02 botón de descarga en la celebración del retorno (urlEntrega solo con PAGADO server-side), F03 rate limit por IP en las 3 superficies. Deploy vía API (webhook GitHub→Vercel CAÍDO, pendiente reconectar), validado con compra real ARMY-16 |
| pago-webhook-amount-string | done 2026-08-18 — incidente de producción RESUELTO: Flow prod serializa amount como STRING y el Gate 5 comparaba estricto ⇒ ningún pago real confirmaba. Fix en el adapter (FlowGetStatusWire + normalizarAmountFlow, fail-closed NaN), deploy ddd3915, rescate de la orden real de iselk (PAGADO, ticket 13, correo delivered a la compradora). Validado después por 3 compras reales más |
| storefront-verificador-tickets | 2026-07-27 (Vitest 64/64 + E2E 9/9 en Chrome CDP propio; deploy `e0cda2f` READY + smoke prod OK, `iselk.sorteatelo.cl/verificar` vivo) — verificador público de tickets: página de sistema `/verificar` en TODAS las tiendas (Comprador busca por correo ⇒ Números del sorteo ACTIVO, solo números+prefijo, cero PII, respuesta indistinguible correo-sin-compras), `checkout.verificarTickets` + primer rate limit del repo (`limiteDeIntentos.ts`, tenant+IP, 429 al 11º), `BoletosDelSorteo` compartido byte-idéntico, links pinned SSR header+footer. Cierre post-deploy con permiso del usuario: 2 drifts de conventions aplicados (enumeración de páginas con tema + sección rate limiting), enlace legal «Bases del sorteo» del footer vuelto incondicional/SSR (ADR-0008 sin JS), y fix estructural del falso negrita (`font-synthesis-weight:none` para pares de un solo peso: Anton/Bebas) |
| selfservice-operador | 2026-07-27 (Vitest 88/88 del carril; E2E autenticado con cuenta Google real SIN membresías — el «desconocido» montó todo solo: alta `e2e-alta-2707` → ToS → producto con archivo real a R2 → Flow BYO cifrado → sorteo → plan pagado en sandbox → PUBLICADA → storefront vivo → invoice espejado) — alta self-service de Tiendas (F08 del roadmap): crearTienda $tx con guard D8, aceptarTos, gate único de publicación, despublicar. **F04 (panel Operador) `superseded`** por ADR-0023. Hallazgos derivados a carriles nuevos: D8 vs. plan ADICIONAL sin camino (una tienda por usuario vs. «segunda a mitad de precio»), ToS 2026-07-17 sin mencionar la suscripción $25.000/mes (→ abogado, F10), 4 tropiezos UX del alta (producto nace Borrador sin aviso, «.tudominio» literal, ~8s de refresco, nav completo sin tienda). Diagnóstico colateral importante: cuenta Google datawalt quedó LINKEADA al User de nikochaima72 (account-linking de NextAuth con sesión activa) — restaurada tal cual; separarla es decisión del usuario |
| plataforma-facturacion-suscripciones | 2026-07-27 (5 pasadas del tester; Vitest 283/283 del alcance + 224/224 post-fixes; **E2E 23/23 contra el sandbox REAL de Flow con cobros reales**; ADR-0026) — módulo completo de facturación de la plataforma: Flow Suscripciones con cuenta PROPIA (mundo separado del BYO-Flow, I1), suscripción por Tienda anclada a Pagador ($25.000 full / $12.500 adicional, UNA full por Pagador con promoción diferida al ciclo nuevo vía cron), gate en publicar sin trial, suspensión escalonada que protege al Comprador (Entitlements/verificador jamás se cortan), cancelación a fin de período, exención CORTESIA/GRANDFATHER con evaluación lazy, cupones con código repartible propio + CLI, 7 correos Resend + cron diario, paso «Activa tu plan» + página Plan. **6 blockers de contrato con la API real cazados y cerrados en sandbox** (redirect sin token, retorno POST→303, duration de cupones invertido, webhook sordo→token/payment/getStatus, espejo por Invoice.status, promoción=delta cobrado al acto→diferida). Follow-ups en Bitácora: alerta ruidosa en impago normal, agotamiento de 3 reintentos jamás ejercido (limitación sandbox), duplicación correos Flow/nuestros (decisión de producto), y el **checklist de go-live en prod** (crear los 2 planes en la cuenta Flow real, `grandfather:tiendas -- --aplicar` ANTES de que el gate quede activo, callback por APP_URL). Código en main vía `56ecc20` (commit de otro carril durante la corrida) |
| checkout-retorno-numeros-sorteo | 2026-07-26 (Vitest 29/29 del alcance + regresión de área 218/219 con 1 rojo AJENO del carril de correos; E2E 9/9 en Chrome propio por CDP — la rama PAGADO se ejerció SIN Flow sandbox: superficie solo-lectura por token, órdenes ya pagadas + fixtures efímeros borrados) — `checkout.estadoOrden` devuelve `{estado, numeros, prefijo}` gateado por PAGADO; boletos-chip en la celebración de `checkout/retorno` (uno por bloque de `bloquesDeNumerosDelSorteo`, byte-idéntico al correo, escala del tenant); 5 fases de copy honesto (`pagado`/`fallido`/`timeout`/`esperando`/`sin_token`) con derivación pura testeada en `src/lib/faseRetornoCheckout.ts` (D6: sin token gana sobre el cap); `landing.faq.005` borrado (F02 volvió verdad su premisa). 4 drifts de frontend-conventions aplicados. Follow-ups: nit `variant` del botón en fases terminales (pulido CTA) + 2 design findings PREEXISTENTES del par de tokens de `momento_ticket` (contraste 3,5:1 en demo-noche; boleto claro en dark — a confirmar como decisión). Commit selectivo solo de archivos propios; `schemas.ts`/`routers/checkout.ts`/`frontend-conventions.md`/INDEX quedan con su carril dueño |
| storefront-tema-paginas-plataforma | 2026-07-26 (119/119 Vitest, 5/6 E2E + 1 parcial; verificación extra del usuario: funnel Flow sandbox completo en `prueba` con checkout/retorno tematizados) — las 5 páginas de plataforma del storefront heredan el tema MÍNIMO de la Tienda (fondo/tipografía/radio/modo) vía `resolverTemaPagina` null-si-default (no-op byte-idéntico); drift de frontend-conventions aplicado. Follow-up candidato anotado en Bitácora: pasar chrome global + navPaginas a las mismas 5 páginas |
| builder-countdown-presencia | 2026-07-26 (28/28 Vitest scoped + suite 1345 con 15 rojos AJENOS, 4/4 E2E en navegador real vía CDP propio) — `urgencia_countdown.estiloVisual` clasico/panel/tarjeta configurable desde el inspector; segundos en las 3 variantes (enmienda D2: clasico también, `54d 03h 12m 07s`, mismo helper; chip del header intacto); `tarjeta` = badge+premio+reloj acentuado+caption+CTA, sin overflow a 320px (blocker del reviewer corregido con E2E que lo mide); reduced-motion apaga decoración sin congelar el reloj; iselk publicado rev 8 en `tarjeta`, otras 6 tiendas intactas; drift Badge-envuelve/card-contenida/Group.gap aplicado a frontend-conventions. Sin commit |
| builder-dreamy-secciones | 2026-07-26 (369/369 Vitest, 1/3 E2E — 2 skips por carriles de navegador ocupados: pasada de píxeles + Select del inspector) — estilo dreamy opt-in en `como_funciona`/`garantias_sorteo`/`faq` (defaults no-op sin v-bump, byte-idénticos); bug de a11y del chevron `＋` cazado y corregido; seeds iselk + demo-dreamy publicados, noche/editorial intactas; drift `styles` vs `classNames` aplicado a frontend-conventions. Gate global con rojos AJENOS (facturación in-flight); los 10 archivos de la tanda limpios. Sin commit |
| guia-conecta-tu-ia | 2026-07-26 — guía «Conecta tu IA» (modal en Conexiones IA, carriles Claude y ChatGPT con capturas) + allowlist OAuth ChatGPT; dance de AMBOS clientes validado en prod (aud no bloquea); 36/36 Vitest |
| mcp-organizador | 2026-07-26 — MCP del Organizador: OAuth 2.1 AS propio (DCR+PKCE+consent+revoke) + 27 tools por membresía + Conexiones IA + audit (ADR-0025); 65/65 Vitest, E2E 7/10 sin rojos |
| landing-reposicionamiento | 2026-07-26 (25/25 Vitest, 9/9 E2E) — reposicionamiento sorteo-first de la landing del apex + paquete SEO técnico: copy sin "rifa" y con Flow mencionado 1 sola vez, 2 bandas nuevas (hazlo-tú-mismo + precio $25.000/mes IVA incl.) en la secuencia D8 de 9 bandas, FAQ ×9 con precio real, JSON-LD derivado del copy (Organization/WebSite/SoftwareApplication+Offer/FAQPage), canonical, robots.txt, sitemap.xml y og.png 1200×630 real. Bug real cazado en review: la landing prometía que el Comprador «ve su número» y ninguna superficie lo muestra. **BACKLOG derivado**: mostrar el número/ticket al Comprador (retorno + correo) + buscador de tickets — al aterrizar hay que borrar el test `landing.faq.005` y reescribir la FAQ 5 + `CONFIANZA[1]` |
| checkout-campos-configurables | 2026-07-25 (261/261 Vitest, 12/12 E2E) — campos de checkout por Tienda: `CheckoutField` + snapshot `CheckoutFieldResponse`, panel, checkout dinámico, validación server-side, detalle de venta y export CSV; change-set-reviewer APPROVE |
| plataforma-retiro-operador | 2026-07-26 — rol Operador extirpado (ADR-0023: MCP god-mode, panel, flag, allowlist; una sola puerta = membresía); 598/598 Vitest, 6/6 E2E |
| admin-multi-tienda | 2026-07-25 — panel scoped por subdominio (ADR-0022, D11 sin acceso Operador cross-tienda) + switcher funcional; 226/226 Vitest, E2E 7/7, deployado en 9499804 |
| admin-bases-pdf-y-limpieza | 2026-07-25 — Vitest verde (37/37 nuevos, 751/754 full con 2 fallos ajenos), E2E 7/7 tras cerrar el gate |
| mvp-roadmap | superseded 2026-07-16 por pivote SaaS → `26-07-16-saas-roadmap.md` (trabajo parcial F01 se rescata en la Fase 1 nueva, ver S8) |
| auth-admin-google | superseded 2026-07-16 por pivote SaaS → `26-07-16-saas-roadmap.md` F05 (se rescatan OAuth/authPolicy/guard; muere la allowlist mono-usuario) |
| efectos-post-pago | superseded 2026-07-16 por pivote SaaS → `26-07-16-saas-roadmap.md` F02 (contrato del hook post-pago sigue válido, re-scopeado por tenant) |
